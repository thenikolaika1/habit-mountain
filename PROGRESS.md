# Habit Mountain — состояние проекта

Трекер привычек с игровыми элементами: гора прогресса, испытания, достижения,
календарь. PWA на чистом JS (без фреймворков и сборщика для самого
приложения), устанавливается на телефон/компьютер, работает офлайн.

Этот файл описывает состояние проекта **после редизайна v2** (5 вкладок,
раздел «Испытания», раздел «Настройки», новая палитра). Актуален на дату
последнего обновления — при следующих правках дополняйте, а не переписывайте
с нуля.

## Функциональность

### Навигация — 5 вкладок (низ экрана, sticky)
`js/components/tabBar.js` — Испытания (`#/challenges`) · Привычки
(`#/habits`) · Прогресс (`#/progress`) · Достижения (`#/achievements`) ·
Настройки (`#/settings`). Роутинг — самодельный, на `location.hash`
(`js/app.js`, `parseRoute()`), без библиотек.

### Испытания
5 испытаний одновременно активны, набор детерминированно меняется 1-го числа
каждого месяца (без сервера и без сохранения выбора — просто чистая функция
от `"YYYY-MM"`, см. `pickMonthlyChallenges()` в `js/logic/challenges.js`).
Правило каждого испытания — функция от месячной статистики (`computeMonthStats()`
в `js/logic/progress.js`): дни месяца, засчитанные по majority-правилу горы,
дни, где выполнены АБСОЛЮТНО все привычки (`.perfect`, отдельно от
majority-правила `.counted`), суммарные отметки, лучшие серии подряд.
Полный пул из 11 испытаний — в `CHALLENGE_POOL`. Испытание засчитывается
автоматически (как ачивка), без отдельного игрового потока — карточка
открывает модалку с описанием и текущим статусом.

Каждое испытание в пуле несёт `difficulty` (`easy`/`medium`/`hard`,
`DIFFICULTY_META` в `challenges.js`). `pickMonthlyChallenges()` набирает
активную пятёрку не плоским шафлом по всему пулу (при котором из-за
перекоса пула месяц мог получить 2 лёгких/1 среднее/2 сложных), а
послойно — ровно 2 лёгких + 2 средних + 1 сложное каждый месяц
(`TIER_COUNTS`), тем же `mulberry32(hashString(monthKey))`-генератором,
своя перетасовка на каждый слой. В пуле 3 лёгких, 6 средних и ровно 2
по-настоящему сложных испытания — `climb_summit` («Штурм вершины» — 100%
прогресса горы за месяц, весь месяц по majority-правилу) и
`perfect_discipline` («Безупречный месяц» — все привычки каждый день
месяца без единого пропуска, по `.perfect`, строже majority-правила).
Прежние «сложные» (33%/50% горы, 40 отметок, 10-дневная серия) по факту
были не намного труднее средних — переведены в `medium`, чтобы
единственный сложный слот месяца доставался только действительно
трудному испытанию. 5 активных карточек сортируются по возрастанию
сложности (`DIFFICULTY_RANK`, стабильная сортировка) на уровне рендера —
порядок выбора/детерминированность месяца не меняется.

Плашка сложности — непрозрачная светлая таблетка (цветной кружок +
подпись «Легко»/«Средне»/«Сложно», зелёный/жёлтый/красный — токены
`--color-accent`/`--color-warning`/`--color-danger`, новых цветов не
заводили) в **левом верхнем** углу карточки поверх фото
(`.challenge-difficulty-badge.media-card-badge--left`) — сплошной фон
специально, чтобы читалось поверх любого места на иллюстрации, не
полупрозрачная надпись в подложке, как было раньше.

В **правом верхнем** углу — круглый прогресс-донат
(`challengeProgressBadge()` в `js/illustrations.js`, обычный
`.media-card-badge` без модификатора): кольцо с дугой-заливкой по
проценту и числом со знаком `%` в центре (например «60%», не голое
«60» — `font-size` текста уменьшен с 11px до 10px в
`css/illustrations.css`, чтобы даже худший случай «100%» помещался в
диск без обрезки, подобрано по замеру `getBBox()` текста против
диаметра диска), либо галочка на сплошном акцентном круге, если
испытание уже пройдено. Считается по `progress(m)` — та же метрика,
что и `check(m)` у правила испытания, только как дробь 0-1, а не булево
(`getChallengeProgress()`). Линейной полоски прогресса в подложке карточки
больше нет — весь прогресс теперь только в этом кольце. Тот же донат и
та же плашка сложности переиспользуются в Достижениях — см. ниже.

Фон карточки — реальное фото награды (кубок/щит/корона/молния/звезда,
`assets/habits/medal_*.png`, все ≈16:9 с наградой по центру). Каждый
месяц активно ровно 5 испытаний — столько же, сколько файлов наград, так
что назначение всегда 1:1 без повторов внутри месяца
(`getMedalForChallenge(id, monthKey)` в `js/logic/challenges.js`): для
одного конкретного месяца (тот, что был активен на момент подбора)
награды расставлены вручную по смыслу названия/описания испытания; для
любого другого месяца (прошлого или будущего, когда пул из 10 испытаний
ротируется на новую пятёрку) — детерминированная случайная перетасовка
тех же 5 файлов, своя на каждый `monthKey`, без ручного подбора при
каждой смене месяца. `challengeHeroForId(id, medalFilename)` в
`js/illustrations.js` остаётся чисто презентационной — берёт уже
вычисленный файл (или `null`) и либо рендерит фото
(`.challenge-hero-photo`, `object-fit: cover; object-position: center`
— один общий рецепт на все 5 файлов, индивидуальная подгонка больше не
нужна), либо, как раньше, рисованную SVG-сцену карабкающейся фигурки;
при отсутствующем/битом файле `wirePhotoFallback()` (вызывается после
рендера в `challengesView.js`/`achievementsView.js`) откатывает `<img>`
на ту же SVG-сцену. В Достижениях та же карточка использует
`getMedalForChallenge(id, info.monthKey)` — месяц, в котором испытание
было пройдено/провалено, а не текущий — поэтому награда там всегда
совпадает с тем, что показывалось на активной карточке в тот момент.
Иконка над заголовком «Испытай себя!» — тоже реальное фото
(`medalHeaderPhoto()`, всегда `assets/habits/flame_icon.png`, квадратное
1:1 фото пламени) в том же 72×72 слоте, где раньше была рисованная
медаль-на-ленте; свой фолбэк на прежнюю SVG при ошибке загрузки, не
связана с назначением наград на карточках (`MEDAL_FILES`/
`getMedalForChallenge()`) — это отдельный, не ротируемый файл.

Заголовок «Испытай себя!» и подзаголовок с датой под ним выделены жирным
градиентным акцентом — и теперь **этот же стиль применён ко всем пяти
заголовкам разделов** (Испытания/Привычки/Прогресс/Достижения/Настройки),
не только к Испытаниям/Привычкам, как было раньше. Раньше это были два
отдельных CSS-правила: общий `.view-header h1` (`font-weight: 600`, без
градиента) плюс скоуп-класс `.challenges-title h1, .habits-title h1`
(жирный градиент), выигрывавший только там, где `challengesView.js`/
`habitsView.js` оборачивали `<h1>` в свой доп. `<div>` — Достижения/
Прогресс/Настройки рендерят `<h1>` прямо в `.view-header`, без такой
обёртки, поэтому у них раньше применялось только общее правило. По
запросу «единый цвет для всех заголовков» эти два правила объединены в
одно — сам `.view-header h1` (`css/base.css`) теперь всегда
`font-weight: 800` + заливка текста градиентом `var(--color-accent)` →
`var(--color-accent-dark)` через `background-clip: text` (тот же
светло-зелёный-к-насыщенному градиент, что на `.btn-primary`/`.fab`).
`.habits-title`/`.challenges-title` divs в разметке остались (просто
структурные обёртки, больше без собственных стилей). Подзаголовок
(`.mountain-month-subtitle`) в это не входит — остаётся как был
(`font-weight: 600` только под `.challenges-title`, `400` у остальных
разделов, включая Прогресс) — запрос был именно про заголовки-`<h1>`.

Смена весов у `.view-header h1` **вскрыла реальный баг с загрузкой
шрифта**: Google Fonts запрашивался только с `wght@600;700`
(`index.html`, `build-preview.mjs` в scratchpad), а раз ни один элемент
страницы больше не рендерится буквально с `font-weight: 600` (только
`800`, резолвящийся в ближайший объявленный вес — `700`), браузер
никогда не лениво-загружает грань `600` — она объявлена в стилях, но
никогда фактически не скачивается. Добавили `800` в список весов
(`wght@600;700;800`) в обоих местах — реальным браузерам с корректным
`User-Agent` Google Fonts отдаёт точную грань 800 (не синтезированную
из 700), что честнее типографически.

### Привычки
Добавление/редактирование/архивация/удаление — `js/state/habits.js`,
`js/views/habitsView.js`. Формы **больше нет ручного выбора фото/иконки
и цвета**: и эмодзи-пикер, и цветовая палитра (7 кружков) убраны из
формы целиком. `habit.icon` теперь всегда назначается автоматически
(round-robin из `DEFAULT_ICONS`, специально без пересечений с
`PHOTO_BY_EMOJI` — иначе случайное совпадение подставило бы чужое
фото), фото/иллюстрация подбираются по названию (см. ниже). Поле
`habit.color` в схеме осталось (создание привычки по-прежнему
назначает его через round-robin фолбэк `PALETTE`/`pickColor()` в
`state/habits.js`, при редактировании остаётся как был), но выбирать
его вручную нельзя, и **нигде в интерфейсе больше не отображается** —
раньше рисовал градиент иконки-чипа в списке «Сегодня»/сводке дня, но
эта иконка-чип сама заменена на универсальное фото (см. ниже) — та же
судьба, что раньше постигла `habit.icon`. Тип
привычки (простая ✓ / числовая #), для числовой — единица измерения из
фиксированного списка (`js/logic/units.js`) выбирается кликом по
пилюле с иконкой-эмодзи (`.unit-picker`/`.unit-picker-item`, `UNIT_ICONS`),
не голым `<select>`, и режим ввода «одно число» или «по подходам»
(сумма нескольких значений за день, `js/views/dayDetailView.js`). Поиск
по названию (фильтрует список без пересборки всего экрана — поле поиска
не теряет фокус). Блок «Популярные привычки» — 6 пресетов, добавляются
в один клик (в обход формы — `icon` им задаётся напрямую в коде).

Фото/иллюстрация карточки — `heroIllustrationForHabit()` в
`js/illustrations.js`, два уровня, и **всегда реальное фото**, ни разу не
общая рисованная гора: (1) по эмодзи — только 6 пресетов
(`PHOTO_BY_EMOJI`), гарантированно не переопределяется категорией; (2)
по **категории, определённой из названия привычки** — `js/logic/categories.js`
(`detectCategory()`): Спорт/Здоровье/Ментальное здоровье/Продуктивность/
Обучение и развитие/Финансы/Дом и быт/Отношения, каждая со своим реальным
фото (`health.png`/`productivity.png`/`learning.png`/`finance.png`/
`home.png`/`relationships.png` — загружены пользователем; Спорт/Ментальное
здоровье переиспользуют уже существующие `otzhimaniya.png`/`meditaciya.png`,
отдельных файлов для них нет). Если название не подошло ни под одну
категорию — фолбэк не на рисованную сцену, а на **нейтральное категорийное
фото** (`NEUTRAL_CATEGORY_ID = "productivity"`), чтобы непойманное имя не
читалось как явный визуальный промах; привычка получает готовую, не
рисованную иллюстрацию в любом случае. (Автоназначенный `icon` — round-robin
из `DEFAULT_ICONS`, больше не выбирается вручную и не несёт смысла — раньше
использовался как третий уровень фолбэка через `HERO_BY_EMOJI`, но это
иногда подставляло случайную SVG-сцену вроде «медитация»/«творчество» по
чистому совпадению эмодзи; этот уровень убран, `HERO_BY_EMOJI`/`heroXxx()`
остались только для фолбэка на битый файл фото, см. ниже.)

Само определение категории — не точное совпадение фразы, а счёт: для
каждой категории считается, сколько её ключевых слов входит подстрокой в
название (регистронезависимо), выбирается категория с максимальным
счётом (при равенстве — та, что раньше в массиве `CATEGORIES`, тот же
приоритет Спорт/Ментальное здоровье выше общего Здоровья, что и раньше).
Списки ключевых слов заметно расширены — не только 50 исходных пресетов,
а широкий обиходный словарь на тему (для Отношений — «звон»/«созвон»/
«близ»/«мам»/«позвонить» и т.п., чтобы «Звонок близким» тоже попадал в
категорию). Полноценные эмбеддинги офлайн, без бэкенда и без сетевых
вызовов из клиента, не вариант — это практическая замена. Категория —
чистая функция от текущего названия, без нового поля в схеме и без
миграции, поэтому подхватывается и для уже существующих привычек, не
только для новых.

`object-position` у `.habit-hero-photo` (`css/illustrations.css`) —
`center 20%` (пробовали `38%`, затем `22%` — оба оказались слишком
агрессивными, см. ниже). Вертикальная координата реально влияет только
на широкую карточку списка `.habit-card` (`aspect-ratio: 2.6/1`) — у
неё `cover` масштабирует квадратное фото по ширине, оставляя
вертикальный запас; у портретных `.popular-habit-card`/`.habit-hero`
масштаб идёт по высоте, там `object-position` математически не при чём
(и так показываются верхние ~85% фото). Видимое окно на `.habit-card` —
жёстко фиксированные ~38.5% высоты исходного фото (следствие аспект-
рейшо контейнера, не зависит от этого значения — оно только двигает
*позицию* окна). У части фото (например `progulka.png`) голова занимает
короткую полосу в кадре — подобрано прямым перебором на реальной
карточке: голова целиком видна вплоть до ~20%, а к 24% исчезает из
кадра полностью (не постепенно обрезается, а целиком уходит за
верхнюю границу окна) — `20%` даёт запас на каждом фото в наборе,
`38%`/`22%` у части фото обрезали или полностью срезали голову.

Три конкретных файла дополнительно точечно скорректированы от этого
дефолта: `meditaciya.png` поднят до `center 14%`, `progulka.png` — до
`center 6%` (у него был отдельный второй заход — первая правка до
`14%` оказалась заметна недостаточно, подняли ещё), `otzhimaniya.png`
опущен до `center 38%` (аналогично — первая правка до `26%` была
недостаточной). Все три подобраны одним методом (перебор значений на
реальной карточке, не расчётом) — и `progulka.png`, и `otzhimaniya.png`
сохраняют лицо целиком в куда более широком диапазоне, чем
использованные значения (`progulka.png` — вплоть до чистого `0%`,
`otzhimaniya.png` — вплоть до `50%`), так что здесь есть большой запас
на случай, если понадобится подвинуть ещё. Остальные фото не трогали —
правило подключено через `[data-photo="имя_файла.png"]`
(`css/illustrations.css`, атрибут выставляется в `heroPhotoFrame()`/
`categoryPhotoFrame()`, `js/illustrations.js`), без нового класса или
поля в схеме, так что затрагивает файл везде, где он показан
(`meditaciya.png` — и пресет «Медитация», и категория `mental_health`;
`otzhimaniya.png` — и пресет «Отжимания», и категория `sport`;
`progulka.png` — только пресет «Прогулка», у него нет
категории-двойника).

Изначально это правило было подключено через `[src$="имя_файла.png"]`
— и молча ломалось именно в Artifact-превью: тамошний бандлер
(`build-preview.mjs`, только в scratchpad, не в репозитории) инлайнит
каждое фото как `data:image/png;base64,...` прямо в `src`, так что
`src` там никогда не заканчивается на `progulka.png`/`otzhimaniya.png`
— селектор просто не совпадал, ошибок или предупреждений при этом не
было, карточка тихо откатывалась на дефолтные `20%`. В самом
задеплоенном приложении (не в превью) бага не было — там `src`
остаётся относительным путём. Раз человек, глядящий на превью, не мог
отличить «правки нет в коде» от «правка в коде есть, но не видна» —
переключили селектор на `data-photo`, отдельный атрибут, который
бандлер не трогает.

Клик по привычке → её персональный экран (`js/views/calendarView.js`):
полноэкранное фото-иллюстрация-шапка (та же `heroIllustrationForHabit()`;
пропорция `4:5` — фото доминирует над экраном, как в референсе) с
названием привычки крупным жирным шрифтом и бейджами (тип/единица,
стрик) поверх затемняющего градиента, под ней — вертикальный список
дней месяца «таблетками» (не сетка) с круглым индикатором статуса
справа (`statusRing()`); клик по доступному дню → что сделано **по
этой привычке** в этот день (`js/views/dayDetailView.js`, редактируемо
в пределах 3-дневного окна, как и раньше — поменялась только вёрстка,
не логика).

Верх экрана «Привычки»: заголовок `<h1>Привычки</h1>` — тот же
жирный зелёный градиент текста, что и «Испытай себя!» в Испытаниях
(`.habits-title h1`/`.challenges-title h1` — общий CSS-блок в
`css/base.css`, чтобы два заголовка не могли разойтись), ниже — поле
поиска с иконкой-лупой. Список привычек — такие же крупные
иллюстрированные карточки-баннеры, что и «Популярные привычки»
(`.habit-card.media-card` — фото/тематическая сцена во всю карточку,
имя+мета поверх затемняющего scrim, кружок-статус и кнопка
редактирования поверх фото), не строка с мелким эмодзи-чипом.
«Популярные привычки» под списком — та же карточка в горизонтальной
ленте, компактнее и в формате портрета (карусель пресетов, не список).

### Прогресс (гора)
`js/views/mountainView.js`, геометрия — `js/mountainSvg.js` (не менялась в
редизайне, только палитра через CSS-переменные). Прогресс — за календарный
месяц: день засчитывается, если выполнено больше половины активных привычек;
шаг = `1 / daysInMonth`; в начале нового месяца — сброс с показом итога
прошлого (`js/components/monthRecap.js`).

Под горой — новый месячный календарь (весь актуальный месяц, без навигации —
раздел всегда показывает текущий). Клик по дню → сводка **по всем активным
привычкам сразу** (`js/views/daySummaryView.js`, `openDaySummary()`) — в
отличие от календаря конкретной привычки (раздел 2), здесь не редактирование,
а обзор дня целиком, без ограничения по «свежести» дня. Список «Сегодня»
(`todayListHtml()`) — у каждой привычки слева маленький круглый значок
(`.today-item-avatar`, `todayAvatarIcon(habit)` в `js/illustrations.js`)
вместо иконки-чипа/эмодзи с `habit.color`/`habit.icon`. Прошёл три
захода: универсальное фото (`productivity.png` для всех — читалось как
заглушка, не осознанная иконка), затем универсальный зелёный кружок с
белой галочкой (читался как *второй* индикатор выполнения рядом с
настоящим чекбоксом справа — визуально путал). Итоговый дизайн —
**по-настоящему персональная** контурная пиктограмма по смыслу
привычки (капля для «Пить воду», книга для «Читать», кровать для
«Сон», гантель для силовых, конверт для почты, телефон для звонков,
монета для финансов и т.д. — 18 тем в
`js/logic/habitIconTopics.js`'s `HABIT_ICON_TOPICS`, подбор через
`pickHabitIconTopic()` — тот же принцип счётного сопоставления ключевых
слов, что и у `detectCategory()` в `logic/categories.js`, но отдельный,
более мелкозернистый набор ключевых слов: `detectCategory()` даёт на
карточку одну из 8 широких фото-категорий, а тут внутри одной такой
категории — например «Здоровье» — нужны визуально разные иконки для
воды/сна/витаминов/зубного врача). Название без совпадений — нейтральная
звёздочка-искра (не галочка — специально, чтобы даже фолбэк не читался
как статус). Кружок — светлый фон (`var(--color-surface-alt)`, тот же
токен, что и у полей ввода — не новый цвет) с тонкой зелёной обводкой
(`2px solid var(--color-accent)`), контур пиктограммы — тем же зелёным,
без заливки: явно читается как декоративный идентификатор, не как
статус (никаких галочек/сплошной заливки внутри — это специально
оговорённое требование). `.today-check` в неотмеченном состоянии тоже
получил зелёную обводку вместо нейтрально-серой — отмеченный чекбокс и
так уже был сплошным зелёным (`.today-item.is-done .today-check`, не
менялось), это единственный настоящий индикатор выполнения, логика
клика/отметки не менялась. Раз аватар больше не `<img>` — не может «не
загрузиться», фолбэк-обвязка (`wirePhotoFallback()`,
`data-fallback-avatar`), нужная для прошлой (фото) версии, убрана
целиком. То же самое — в сводке дня (`daySummaryView.js`, read-only
вариант, тот же `todayAvatarIcon(habit)`).

Испытания (`js/views/challengesView.js`) и Достижения — тоже карточки-баннеры
(`.challenge-card`/`.achievement-card.media-card`): вместо иконки-чипа —
тематическая иллюстрация «фигура-альпинист на горе приложения»
(`challengeHeroForId()`, `js/illustrations.js`) — переиспользует ту же
геометрию горы/склона, что и сам маскот приложения, вместо произвольной
сцены. У Испытаний в scrim — заголовок+описание, у Достижений — заголовок+
дата разблокировки (не описание, оно избыточно для уже пройденного).

### Достижения
`js/views/achievementsView.js` — «Пройденные испытания»
(`CHALLENGE_POOL`, отфильтрован по `meta.completedChallenges`), карточки —
см. выше. Постоянные ачивки (`js/logic/achievements.js`, `ACHIEVEMENTS`)
больше нигде не отображаются — экран с ними убрали как визуально лишний, но
сама разблокировка (`evaluateAndUnlock()`, ratchet,
`meta.unlockedAchievements`) по-прежнему считается и хранится в фоне на
каждый рендер, просто без UI; восстановить показ — тривиально, код не удалён.

Ниже — «Незавершённые попытки» (только если такие есть): испытание, чей
активный месяц закончился без выполнения, не пропадает молча — при
переходе на новый месяц (`maybeShowMonthRecap()` в
`js/components/monthRecap.js`, тот же ratchet по `meta.lastSeenMonth`, что
и у итогов горы) `archiveEndedMonthChallenges()` в `js/logic/challenges.js`
пересчитывает финальную месячную статистику **закончившегося** месяца
заново из сырых данных (не из того, что случайно осталось в кэше) и для
каждого из 5 испытаний, что были активны в том месяце:
- если уже пройдено (в этом же месяце или раньше) — ничего не делает;
- если по факту порог достигнут, но приложение не было открыто, чтобы
  поймать это вживую — засчитывает испытание задним числом (тот же путь,
  что и `evaluateAndUnlockChallenges()`, применённый к статистике
  закончившегося месяца, не текущего);
- иначе — сохраняет `meta.incompleteChallenges[id] = { monthKey,
  progress, endedAt }` (`getIncompleteChallengesMap()`), одна запись на
  испытание (последняя неудачная попытка, не полная история всех
  промахов) — очищается автоматически, если это же испытание когда-нибудь
  всё-таки будет пройдено.

Карточка незавершённой попытки — та же иллюстрация испытания, что и на
активной карточке (`challengeHeroForId()`), но приглушённая
(`.achievement-card--incomplete`, `grayscale`+`brightness` на SVG — читается
как «не получилось» рядом с яркими пройденными карточками), с теми же
двумя угловыми бейджами, что и на активной карточке (плашка сложности
слева; прогресс-донат справа, застывший на итоговом проценте месяца), и
подписью «Не завершено — прогресс NN% (месяц год)» в подложке вместо
даты получения.

### Настройки
`js/views/settingsView.js`: тема (светлая/тёмная/системная — пишет
`meta.settings.theme`, применяется через `data-theme` на `<html>`,
`js/logic/theme.js`), единица измерения по умолчанию для новых числовых
привычек, уведомления (честно: без сервера фоновые пуш-напоминания
невозможны — переключатель запрашивает `Notification.requestPermission()` и
показывает тестовое локальное уведомление), экспорт всех данных в `.json`
(`Blob` + `<a download>`), «О приложении», и «Сбросить аккаунт» (двойное
подтверждение через `openConfirm()`, затем `localStorage.clear()` +
перезагрузка). Каждая строка — с иконкой-чипом слева (`.settings-row-icon`,
тот же градиентный сквиркл, что у карточек привычек, но нейтрального
зелёного цвета, не привязан к конкретной привычке; у «Сбросить аккаунт» —
красный градиент вместо зелёного, `--color-danger`/`--color-danger-dark`).
Полноразмерная иллюстрация здесь была бы неуместна — строка настройки это
переключатель/список/кнопка, а не карточка «активности», в отличие от
привычки/испытания.

## Архитектура

- **Всё производное, ничего не кешируется.** `js/state/derive.js`'s
  `getAppStats()` — единственная точка входа для «текущих чисел»:
  пересчитывает статистику, ачивки и испытания заново на каждый вызов из
  сырых `habits`+`entries`. Ничего, кроме факта разблокировки, не хранится.
- **Хранилище** — `js/state/storage.js`, ключ `habit-mountain:v1` в
  `localStorage`, `SCHEMA_VERSION = 2`. `emptyState()`/`migrate()` —
  defensive-миграция, переживает данные без новых полей (старые привычки без
  `icon` получают дефолт, старые `meta` без `completedChallenges`/
  `incompleteChallenges`/`settings` достраиваются). Пуб-саб
  (`subscribe`/`saveState`) — любой `saveState()`
  триггерит полный ре-рендер текущего экрана.
- **Рендер** — каждый экран делает `container.innerHTML = ...` целиком на
  каждое изменение состояния. Исключение — `mountainView.js`
  (`renderFreshMountainView`/`patchMountainView`): на горе нужен настоящий
  CSS `transition` для золотой тропы, а это требует, чтобы DOM-узел
  `<path>` не пересоздавался — поэтому там точечный patch вместо полной
  пересборки, если экран уже смонтирован.
- **Модалки/тосты** — `openModal`/`openConfirm` (`js/components/modal.js`,
  кастомная реализация, не `window.confirm()` — тот молча блокируется в
  sandboxed iframe) и `showToast` (`js/components/toast.js`) — единственные
  способы показать диалог/уведомление во всём приложении.
- **Иллюстрации** — `js/illustrations.js`, вручную нарисованные inline-SVG
  (тот же приём, что и гора в `mountainSvg.js`) — без внешних файлов и
  библиотек иконок, чтобы офлайн-режим не зависел от лишних загрузок.
  Включает переиспользуемый `statusRing({state})` (done/empty/locked —
  общий круглый индикатор для списка привычек, карточек испытаний и
  списка дней привычки).
  - `heroXxx()`/`heroIllustrationForHabit()` — 8 детализированных сцен для
    шапки экрана привычки и карточек привычек: не одна фигура-пиктограмма,
    а фигура человека (голова/волосы/тело/руки — общий «словарь» частей,
    см. ниже) в характерной обстановке (спящий в кровати с подушкой и
    одеялом, читающий в кресле с торшером, медитирующий на коврике,
    едящий за столом и т.д.) — «реализм» здесь означает многосоставную
    плоскую сцену, а не фотографию: рисованный SVG остаётся принципиальным
    решением ради офлайн-режима.
  - **Реальные фото — там, где они есть.** `heroIllustrationForHabit()`
    сначала смотрит в `PHOTO_BY_EMOJI` (6 эмодзи из «Популярные привычки» —
    💧/📚/🧘/💪/🛌/🏃) и, если для эмодзи привычки есть файл, рендерит
    `<img src="./assets/habits/<файл>.png">` вместо SVG-сцены — сопоставление
    по эмодзи, а не по названию, так что любая привычка пользователя с тем
    же эмодзи тоже получит фото. Сами 6 файлов лежат в `assets/habits/`
    (загружены напрямую через GitHub — модель не может сохранять вложения
    из чата на диск в этой среде, см. `assets/habits/README.md`) с именами
    из README, ничего в коде дополнительно менять не пришлось.
    `.habit-hero-photo` (`css/illustrations.css`) кадрирует фото
    (`height: 118%; object-position: top`), отрезая нижние ~15% — там, где
    у референсных фото есть своя подпись-таблетка — заголовок карточки
    приложение по-прежнему рисует само поверх затемняющего scrim, тем же
    стилем, что и у карточек без фото. `wirePhotoFallback()` (вызывается
    после каждого рендера в `habitsView.js`/`calendarView.js`) слушает
    `error` на `<img>` и при сбое подменяет его на **ту же тематическую**
    SVG-сцену (droplet для 💧, книга для 📚 и т.д., не общий фолбэк) — карточка
    никогда не остаётся с сломанной/пустой картинкой, даже если файл
    отсутствует, переименован или битый.
  - `challengeHeroXxx()`/`challengeHeroForId()` — то же самое для карточек
    испытаний/достижений: фигура-альпинист на разной высоте склона горы
    (переиспользует geometрический язык самой горы приложения — visual
    tie-in с брендом, не произвольная сцена), пул из 7 вариаций на 10
    `CHALLENGE_POOL`-испытаний.
  - Общий «словарь» частей фигуры (CSS-классы в `css/illustrations.css`):
    лицо — всегда светлый круг с тёмным контуром (`.habit-hero-face`,
    работает и на дневных, и на ночной сцене), волосы/предметы — `--color-
    ink` (`.habit-hero-hair`), одежда/акценты — зелёный (`.habit-hero-prop-
    accent`), конечности — толстый скруглённый штрих (`.habit-hero-limb`).
    Ничего нового в палитру не добавлено — только новые формы поверх тех
    же токенов.
  - Мелкие монохромные `iconXxx()` (поиск, настройки-строки — тема, единица
    измерения, уведомления, экспорт, удаление, инфо) — в духе уже
    существующих `iconMedal()`/`iconHabits()` таб-иконок.
- **Визуальный стиль карточек** — карточки-баннеры (`.habit-card`,
  `.challenge-card`, `.achievement-card`, `.popular-habit-card`) — общий
  шелл `.media-card`/`.media-card-scrim`/`.media-card-badge`/`.media-card-
  icon-btn` в `css/components.css`: тематическая иллюстрация во всю
  карточку как фон, затемняющий scrim снизу с именем/статусом белым
  текстом, статус-индикатор и (где нужно) кнопка редактирования — поверх
  фото, абсолютным позиционированием. Заменяет и изначальные «эмодзи в
  кружке на плоской карточке», и промежуточный вариант «таблетка с иконкой-
  чипом» — карточка теперь несёт тему через саму иллюстрацию, а не через
  отдельный маленький значок.
  - Иконка-чип (сквиркл, статичные SVG-иконки — `iconTheme()`/
    `iconUnit()`/и т.п.) осталась в строках Настроек
    (`.settings-row-icon`) — остаются компактным списком переключателей,
    не карточками «активности»; там `habit.color`/градиент ни при чём,
    свои собственные иконки. `js/state/habits.js` по-прежнему
    экспортирует `darkenColor()` (затемняет hex-цвет по HSL), но
    сейчас нигде не вызывается — список «Сегодня», единственный
    оставшийся потребитель, тоже сменил иконку-чип на универсальный
    круглый фото-аватар с зелёной обводкой (позже, отдельным запросом,
    см. раздел «Прогресс» выше); функция оставлена как есть, а не
    удалена, — тот же принцип, что и у `habit.color`/`habit.icon`.
  - `.fab`/`.btn-primary`/`.stat-value` — зелёный градиент вместо плоской
    заливки, как и раньше. `.fab`'s «+» — фиксированный `#ffffff`, не
    `var(--color-accent-contrast)` (та переменная в тёмной теме почти
    чёрная — предназначена для текста на *светлом* dark-theme-акценте
    вроде `.btn-primary`, а не для этой кнопки, где нужен именно белый
    независимо от темы); `line-height: 1` на `.fab`, чтобы глиф «+» не
    смещался на пару пикселей от геометрического центра круга строчной
    высотой текстового узла по умолчанию.
- **Анимации** — `css/base.css`: переход между экранами — плавный stagger
  прямых детей `.view` сверху вниз (`view-fade-in` keyframes,
  `#view-container.view-transition .view > *`, включается только на
  настоящую навигацию, не на любой ре-рендер — см. `lastRouteKey` в
  `js/app.js`). Галочка при отметке привычки — `check-pop` bounce
  (`css/components.css`). Тост ачивки/испытания — spring-переход. Все
  анимации/переходы уважают `prefers-reduced-motion`. Hover (мышь/трекпад,
  не тач — `@media (hover: hover) and (pointer: fine)`) — увеличение
  1.03–1.1 + зелёное свечение снизу (`rgba(var(--color-accent-rgb), …)`) на
  кнопках, карточках привычек и вкладках.
- **Палитра** — `css/variables.css`: белый/светло-зелёный/зелёный/чёрный
  (`--color-ink`), с точечными акцентами (золото на вершине горы, красный
  для опасных действий). Тема — три состояния (системная через
  `@media (prefers-color-scheme)`, явный выбор через `[data-theme]`,
  описано в `artifact-design`-стиле, теперь применяется к самому
  приложению, не только к бандлу предпросмотра).
- **Google Fonts (Fredoka)** — акцентный шрифт для заголовков экранов,
  крупных чисел статистики, подписей на горе и заголовков на карточках-
  баннерах (`.media-card-title`/`.habit-hero-title`/`.popular-habit-card-
  name` — белый текст на фото-scrim'е, та же логика «крупный акцентный
  заголовок», что и у экранных `<h1>`) — `--font-display`; обычный текст
  (описания, мета-строки, кнопки, названия вкладок) остаётся на системном
  шрифте. Подключён не блокирующим
  способом (`media="print"` → `onload="this.media='all'"`), чтобы медленная
  или недоступная Google Fonts не задерживала первый рендер приложения —
  по той же причине регистрация service worker не ждёт `window.load`.
- **Офлайн/PWA** — `sw.js` (кеш `habit-mountain-v3`, список файлов в
  `APP_SHELL` обновлён под редизайн), `manifest.json` — не менялись
  структурно, только список закешированных файлов пополнен новыми.
  `fetch`-обработчик — cache-first (совпадение в кеше отдаётся без
  повторного похода в сеть), поэтому `CACHE_NAME` нужно бампать при
  каждой правке любого закешированного файла — иначе у всех, кто уже
  открывал приложение (не через Artifact-превью, там сервис-воркера
  нет вовсе), правки просто не появятся, сколько ни переразворачивай.
  `v2→v3` — это как раз такой пропущенный бамп: несколько раундов
  правок `css/illustrations.css` (кроп фото-карточек) и других файлов
  в рамках доработки «Привычки» ушли без бампа кеша.

## Известные ограничения (осознанные)

- Уведомления — только тестовое локальное, реальных фоновых пуш-напоминаний
  без сервера быть не может (честно указано в самой настройке).
- Испытания оцениваются по месячной статистике, а не по «живому» игровому
  потоку — кнопка «Начать» открывает модалку с описанием/прогрессом, не
  отдельный экран с уникальной механикой на каждое испытание.
- Бандл предпросмотра (Artifact) не включает service worker (в
  однофайловом бандле нет `./sw.js` для регистрации) и не может скачивать
  файлы (sandboxed-iframe блокирует `<a download>`) — это ограничения среды
  предпросмотра, не баги самого приложения; в реальном деплое всё работает.
- Если `assets/habits/*.png` когда-нибудь пропадёт/переименуется — не
  сломается, а тихо откатится на рисованную SVG-сцену через
  `wirePhotoFallback()` (см. выше), как и для любой привычки без фото.

## Тестирование

Плейрайт-скрипты (не в репозитории — в scratchpad сессии): полный
регрессионный набор + тесты редизайна (испытания, настройки,
привычки/аватар/поиск/пресеты, кросс-привычечный календарь горы, упрощённые
достижения/таблетки-карточки/герой-экран привычки, иконки-чипы/лента
популярных привычек/пропорции героя v4, карточки-баннеры везде + иконки-чипы
в Настройках/Сегодня v5, реальные фото привычек + фолбэк) — 223 проверки в
основном наборе (не зависят от наличия файлов в `assets/habits/`) + 24
проверки специально для фото-привычек-фичи (подключение фото на карточках —
19, фолбэк на SVG при отсутствующем/битом файле — 3, фолбэк именно на
*тематическую* сцену, а не общий фолбэк — 2) + 15 проверок
сложности/прогресса испытаний (сортировка карточек по возрастанию
сложности, плашка сложности в левом углу/прогресс-донат в правом, верный
процент и галочка при завершении, ретроактивное завершение/архивация
незавершённой попытки при смене месяца, тот же вид бейджей в Достижениях,
устойчивость к повторной загрузке) + 39 проверок специально для
фото-наград-испытаний/баланса сложности/знака %/акцентного заголовка
(все 5 карточек текущего месяца показывают именно ручную по смыслу
награду, без повторов, `object-position: center`, фото реально
грузится; шапка «Испытай себя!» показывает именно `flame_icon.png`
вместо SVG; фолбэк на тематическую SVG-сцену/на `medalIllustration()`
при битом файле; для нескольких других `monthKey` — 5 различных наград
без повторов, вычисленных напрямую через `getMedalForChallenge()`;
карточка в Достижениях показывает ровно ту награду, что вычисляется для
месяца, когда испытание было пройдено/провалено; ровно
2×«Легко»/2×«Средне»/1×«Сложно» среди активных карточек, и единственная
сложная — по-настоящему трудная `perfect_discipline`; донат-текст
оканчивается на `%` и «100%» помещается в диск без обрезки по замеру
`getBBox()`; `perfect_discipline` требует именно всех привычек каждый
день — один пропущенный день не даёт 100%, majority без хотя бы одного
полного дня вообще не даёт прогресса; заголовок «Испытай себя!» —
`font-weight >= 700` и именно зелёный `var(--color-accent)` →
`var(--color-accent-dark)` градиент текста (тот же, что на
`.btn-primary`/`.fab`), подзаголовок заметно жирнее дефолта, а остальные
экраны с тем же `.view-header h1`/`.mountain-month-subtitle` остаются на
`font-weight: 600` без градиента — скоуп не протекает) + 32 проверки
специально для авто-фото привычек по категории/убранного пикера/пилюль
единиц измерения (заголовок «Привычки» — тот же градиент, что у
«Испытай себя!», побайтово; в форме нет ни поля «Иконка», ни `<select>`
для единицы — только ряд пилюль с иконкой-эмодзи; сохранённая привычка
всё равно получает автоматическую иконку без пересечения с
`PHOTO_BY_EMOJI`; 7 разных названий привычек — каждое даёт ожидаемую
категорию/файл на карточке; все 6 пресетов «Популярные привычки»
по-прежнему используют свои собственные фото, не переопределяются
категорией; `object-position` сдвинут с чистого `top`; битый файл
категории откатывается на тематическую SVG-сцену (`heroFitness`/
`heroMindfulness` для Спорта/Ментального здоровья, `heroDefault()` для
остальных пяти) + 9 проверок специально для убранного цветового
пикера (нет `#habit-color-picker`/подписи «Цвет» ни в форме
добавления, ни в форме редактирования; новая привычка всё равно
получает валидный `#rrggbb`-цвет из `PALETTE` через фолбэк;
редактирование привычки не меняет её `color`; иконка-чип в «Сегодня»
по-прежнему рисует градиент на основе `habit.color`) + 13 проверок
специально для более умного `detectCategory()`/нейтрального фолбэка/
глубже кропа (10 названий вне исходных 50 пресетов — каждое верно
определяет категорию через новый счётный подбор и расширенные
синонимы, включая «Звонок близким»/«Видеозвонок бабушке»/«Прочитать
книгу перед сном» — конфликт с «Здоровье» по слову «сном» намеренно
не срабатывает, проверено; название вне всех категорий рендерит
именно `productivity.png`, а не SVG-сцену; `object-position` сдвинут
с чистого `top` — конкретное число этой проверки следом откатили,
см. ниже) + 6 проверок на точечные per-photo правки `object-position`
(`meditaciya.png` → `14%`, `progulka.png` → `6%`, `otzhimaniya.png` →
`38%` — оба последних значения обновлены повторно после того, как
первый заход (`14%`/`26%`) оказался заметно недостаточным; остальные
фото — контрольная проверка, что остались на дефолтных `20%`) + 1
проверка-предохранитель конкретно от бага с бандлером Artifact-превью
(см. выше про `data-photo`) — собирает `<img>` с `src`, уже
переписанным на `data:` URI (как это делает `build-preview.mjs`), и
проверяет, что нужный `object-position` всё равно применяется + 5
проверок на единый градиент заголовков и `.fab` (Прогресс/Достижения/
Настройки дают побайтово тот же `fontWeight`/`backgroundImage`, что и
Привычки; цвет «+» ровно белый; оптический центр «+» — расхождение
меньше пары пикселей по каждой оси через `Range.getBoundingClientRect()`
глифа против `getBoundingClientRect()` самой кнопки) + переделка блока
«Сегодня» — сначала под универсальное фото (`test-redesign-v4.mjs`:
аватар — настоящий круг, не сквиркл; это `<img>`, не градиент-спан;
конкретно `productivity.png`; зелёная обводка; тот же файл у разных
привычек; зелёная обводка на неотмеченном чекбоксе; `test-no-color-picker.mjs`:
аватар теперь `<img>`; новый `test-today-avatar-fallback.mjs` на битый
файл — 6 проверок), затем (той же сессией, отдельным запросом) с фото
на плоскую иконку-галочку — фото на каждой строке читалось как
случайная заглушка, а не осознанная иконка. `test-redesign-v4.mjs`'s
проверки на `<img>`/`src`/фолбэк переписаны на «это `<span>` с
SVG-галочкой (`.today-item-avatar-check`) и фирменным градиентом,
разметка идентична на разных привычках» — тот же счёт проверок в файле
(замена 1:1, не добавление); `test-no-color-picker.mjs` аналогично
переписан на «показывает универсальную галочку, не градиент от
`habit.color`»; `test-today-avatar-fallback.mjs` удалён целиком — весь
файл проверял откат `<img>` при ошибке загрузки, а SVG-значок не может
«не загрузиться», так что фолбэк-сценарий для этого случая больше не
существует (-6 к общему счёту) — итого 349. Следом (той же сессией,
персональные пиктограммы по смыслу привычки вместо универсальной
галочки) — `test-redesign-v4.mjs`'s проверки на градиент/галочку снова
переписаны: светлый фон + зелёное кольцо + контурная (не залитая)
пиктограмма, никакого `.today-item-avatar-check`, разные привычки дают
*разные* пиктограммы (раньше специально проверялось обратное — что
иконка одинакова у всех) — тот же счёт файла, замена 1:1, +4 новые
проверки на стиль контура/фона/кольца; `test-no-color-picker.mjs`
аналогично переписан на «показывает пиктограмму темы, не градиент от
`habit.color`»; новый `test-habit-icon-topics.mjs` — юнит-проверка
`pickHabitIconTopic()` (`js/logic/habitIconTopics.js`) на одно название
на каждую из 18 тем + несколько случаев на конкретные грамматические
ловушки (например «Проверить почту» — стем «почта» без учёта склонения
не поймал бы «почту»; «Работа над проектом» — аналогично для «работа»;
«Йога перед сном» — «сном» намеренно не задевает тему sleep из-за
беглой гласной в «сон→сна→сну→сном», так что побеждает sport по
«йога») + рендер-проверка на `todayAvatarIcon()` (разные пиктограммы
для разных тем, соответствие непойманного/пустого/отсутствующего
названия одному и тому же нейтральному фолбэку) — 27 проверок — итого
380, все проходят на момент последнего обновления с реальными файлами
в `assets/habits/`.

`38%` из предыдущего абзаца оказался слишком агрессивным на реальных
фото — обрезал лица на `home.png`/`meditaciya.png`/`health.png`, а
`progulka.png` вовсе терял голову целиком. Откатили на `20%`,
подобранное прямым перебором по карточке (не расчётом) — см. абзац
про `object-position` выше по файлу.

Редизайн «Календаря месяца» (раздел Прогресс, `css/calendar.css` +
`js/views/mountainView.js` + новая `dayProgressRing()` в
`js/illustrations.js`) — девять точечных правок по одному ТЗ:
ячейки дня получили градиентную заливку + `box-shadow` вместо плоского
белого квадрата (объём), радиус скругления вырос с `--radius-sm` до
`--radius-lg` (тот же токен, что у `.media-card`/карточек привычек),
добавлен `:active`-скейл для тапа на тач-устройствах вдобавок к уже
существовавшему `:hover`-скейлу под `(hover: hover)`; сегодняшний день
теперь заливается мягким полупрозрачным зелёным градиентом
(`rgba(var(--color-accent-rgb), …)`) вместо `inset`-обводки — если
день к тому же уже выполнен (`is-done`), сплошная зелёная заливка
`is-done` намеренно перекрывает мягкий тинт `is-today` (более
конкретный статус важнее); выбранный (кликнутый) день получил
отдельный класс `is-selected` — плотное `accent-dark`-кольцо
(`box-shadow: inset …`) + бледная `--color-surface-alt` заливка,
явно отличимое от заливки «сегодня»; будущие дни (`cell.isFuture` —
уже готовое поле из `buildMonthGrid()`, ничего нового считать не
пришлось) приглушены до `opacity: 0.45` и серого текста; подписи ПН-ВС
перекрашены в `--color-accent`, СБ/ВС дополнительно — в
`--color-accent-dark` (тот же зелёный, на оттенок темнее, а не
посторонний цвет); вокруг числа каждого прошедшего/сегодняшнего дня с
хотя бы одной выполненной привычкой рисуется тонкое кольцо-прогресс
(`dayProgressRing(pct)` — обычный `<circle>` + `stroke-dasharray`, без
`<defs>`/градиентов с `id`, поскольку в сетке рендерится до 31 такого
SVG одновременно; тот же приём, что уже использует `statusRing()`);
`pct = completedCount / activeHabits.length`, доступный только
благодаря новому 4-му параметру `progressCalendarHtml(…,
activeHabitsCount)`/`progressCellHtml(…, activeHabitsCount)` — раньше
это число нигде не доходило до рендера календаря; для дня без единой
выполненной привычки (`pct === 0`) кольцо не рендерится вовсе (не
пустое/бледное — просто отсутствует, оба варианта разрешало ТЗ); для
будущих дней кольцо не считается вообще, даже если у них технически
есть запись в `monthStats.days`. Состояние «какой день выбран» —
`selectedDateKey`, обычная модульная переменная в `mountainView.js` по
образцу уже существовавшей `previouslyDoneToday`: не персистится и не
идёт через `render()`, поэтому подсветка выбора переживает
не связанный с ней ре-рендер экрана (например, отметку привычки).
Проверено, что `.calendar-day`/`.calendar-cell`/`.calendar-grid` нигде
кроме `mountainView.js` не используются (в частности, календарь
одной привычки в `calendarView.js` строится на полностью других
классах — `.day-pill`/`.day-pill.is-locked`), так что новые правила
можно было писать без доп. скоупинга. Новый `test-calendar-redesign.mjs`
(20 проверок: объём/скругление ячеек, переход на `:active`, заливка
«сегодня» до и после `is-done`, `is-selected` отличается от `is-today`
по `box-shadow`, приглушение будущих дней, цвет ПН-ПТ и отдельно СБ/ВС,
кольцо-прогресс есть/её дасэррей ненулевой/число дня всё ещё видно,
кольца нет ни у прошедшего дня без выполненных привычек, ни у
будущего) — итого 400, все проходят. `sw.js`'s `CACHE_NAME` — v3 → v4.

Редизайн «Горы» (раздел Прогресс, `js/mountainSvg.js` + `css/mountain.css`
+ гора-only токены в `css/variables.css`) — четыре точечных правки по
одному ТЗ, плюс проверка/усиление читаемости подписей:

- **Небо**: было плоским 2-стопным CSS-градиентом на `.mountain-wrap`
  (`--stage-sky-top` → `--stage-sky-bottom`, не часть SVG). Стало
  3-стопным (`--stage-sky-bottom` → новый `--stage-sky-mid` → `--stage-
  sky-top`), снизу вверх, плюс `radial-gradient` со свечением
  (`--stage-sky-glow`, новый токен) у самой вершины. Светлая тема —
  мятно-голубой низ, тёплый золотисто-персиковый верх; тёмная — холодный
  тёмно-тил низ, тёплый янтарный «алгоу на закате» верх, свечение
  заметно тусклее.
- **«Тёмный клин»-баг**: оказался не отдельным элементом, а плоским
  `<rect class="mountain-layer-rocks">`, залитым почти-чёрным
  `--stage-rocks` (`#333a33`/`#161915`), обрезанным клипом до клина у
  вершины. Убрали все 4 плоских `<rect>` (base/forest/rocks/summit) —
  теперь один `<rect class="mountain-body">` на весь клип, залитый одним
  SVG `<linearGradient id="mountainBodyGradient">` (8 стопов, попарные
  «ядра» + мягкие переходы: трава → лес → скалы → снег, без резких
  границ). `--stage-rocks` перекрашен в холодный серо-зелёный
  (`#7c8f92`/`#46564f`) — сам источник «бага» устранён на уровне цвета,
  не только структуры. Мёртвый токен `--stage-summit-dark` переиспользован
  под «снег с голубым отливом» — предпоследний стоп перед чистым белым.
  Цвета стопов — не инлайн в JS-шаблоне, а классами (`.mountain-gradient-
  stop--grass/forest/rock/snow-tint/snow`) в `mountain.css`, тот же
  подход, что уже был у `.mountain-tree`. Удалены как мёртвые
  `.mountain-layer-*`/`.mountain-rock-shadow`/`.mountain-summit-shadow`
  (не рендерились, ни один тест на них не завязан).
- **Ёлки**: было 6 одинаковых двухъярусных силуэтов (различался только
  x-джиттер), плоская заливка `--stage-forest-dark`. Стало 3 функции-
  силуэта (`firTree` — исходная высокая узкая, `roundTree` — пушистая
  трёхъярусная пошире, `slimTree` — тонкий одноярусный шпиль),
  детерминированный не-монотонный набор вариантов+масштабов на 6
  прежних позициях (`TREE_VARIANTS`), общий `<linearGradient
  id="treeGradient">` (тёмный низ `--stage-forest-dark` → светлее верх
  `--stage-forest`, оба токена уже существовали) для объёма.
- **Тропа**: непройденная часть (`.mountain-trail`) — тоньше
  (3→2.5), пунктир воздушнее (`2 10`→`2 11`), заметно бледнее
  (opacity 0.85→0.55). Пройденная (`.mountain-trail-walked`) — чуть ярче
  золото (`#f2c14e`→`#f7c948`), толще (4→4.5), двухслойный glow
  (`drop-shadow` core+halo вместо одного слоя 3px/.55) — контраст
  «пройдено vs осталось» подчёркнут сильнее.
- **Читаемость подписей**: `.milestone-label`/`.milestone-label--
  success` уже имели читаемый приём (обводка текста цветом
  `var(--color-bg)`, тема-зависимая) — добавлен ДОП. `filter: drop-
  shadow(...)` под текст, ничего не убрано/не переписано. Логика
  разблокировки (`milestone--reached`, пороги `p`, тексты меток,
  `.mountain-marker` count===0) не тронута — проверено тестами.
- Попутно: `.mountain-progress-caption`'s тёмная тема раньше
  переопределялась только через `@media (prefers-color-scheme: dark)`
  без зеркального `:root[data-theme="dark"]` — на явном выборе тёмной
  темы при светлой ОС подпись оставалась светлой. Добавлено зеркальное
  правило (та же трёх-состояний схема, что и в `variables.css`).
- Новый `test-mountain-redesign.mjs` (21 проверка: слои неба, структура
  градиента горы/её стопы, отсутствие старых плоских `<rect>`, цвет
  «скал» больше не почти-чёрный, >6 полигонов-ёлок с общим градиентом и
  разбросом размеров, двухслойный glow тропы + бледность непройденной
  части, `drop-shadow` на подписях, сохранённая логика разблокировки на
  0%/100%, отсутствие `.mountain-marker`) — итого 421, все проходят.
  `sw.js`'s `CACHE_NAME` — v4 → v5.

Доработка календаря + горы (та же сессия, следующим заходом) — три
точечных правки поверх уже готового редизайна:
- **«Сегодня» в календаре** (`css/calendar.css`, `.calendar-day.is-
  today`) — жаловались, что бледно. Alpha заливки поднята с
  `0.26/0.13` до `0.55/0.3`, плюс добавлено сплошное `box-shadow: inset
  0 0 0 2px var(--color-accent)` (раньше у `is-today` вообще не было
  своей обводки) — заметно выделяется среди дней месяца, при этом
  остаётся явно светлее сплошной заливки `is-done` и другого оттенка,
  чем `accent-dark`-кольцо `is-selected` (оба соседних состояния не
  трогались).
- **Покачивание ёлок** (`js/mountainSvg.js`, `css/mountain.css`) —
  `treeShape()` оборачивает уже собранные полигоны каждого дерева в
  `<g class="mountain-tree-sway" style="animation-duration:…;
  animation-delay:…;">` — `@keyframes tree-sway` (`rotate(-3deg)` ↔
  `rotate(3deg)`), `transform-box: fill-box` + `transform-origin:
  center bottom`, так что точка поворота — низ собственного bounding
  box дерева, без ручного прокидывания пиксельных координат основания.
  Длительность/задержка — детерминированные по индексу (не
  `Math.random()`, тот же приём, что и x-джиттер деревьев рядом), с
  отрицательной задержкой, чтобы все 6 деревьев не начинали качание в
  одной фазе. Отключается под `prefers-reduced-motion: reduce` (тот же
  паттерн, что уже применён к `.mountain-trail`/`.milestone--summit`
  чуть выше в файле).
- **Детали пустых зон** (`js/mountainSvg.js`, `css/mountain.css`):
  облака — новая `cloudsMarkup()`, 3 группы по 3 перекрывающихся
  `<ellipse>` (мультяшный силуэт), рендерятся в небе до клипованного
  тела горы (не обрезаны по силуэту), с собственным медленным
  `cloud-drift` (`translateX` туда-обратно, разная длительность/
  задержка на облако) — в тёмной теме `opacity` снижена (0.6→0.22, та
  же пара `@media`/`[data-theme="dark"]`, что уже применяется к
  `.mountain-progress-caption`). Акценты на каменной зоне — новая
  `mountainDetailsMarkup()`: 3 камня (`<circle>`, `--stage-rocks-dark`
  — токен существовал, но нигде не рендерился до этой правки), 2
  кустика (по 3 маленьких `<ellipse>`, `--stage-forest-dark`, тот же
  мультяшный приём, что у облаков, только мельче и зелёным), 2 снежных
  пятна на камне (`<ellipse>`, `--stage-summit` at `opacity:0.55`).
  Блики на снеговой шапке — 2 тонких `<line class="mountain-snow-
  sparkle">` (`stroke:#fff8e6`, тот же тёплый кремовый, что уже у
  `.mountain-trail`/`.milestone-dot`). Все точки посчитаны через уже
  существующий `pointAtProgress(p)` + фиксированный сдвиг `dx,dy` вниз
  от линии хребта — гарантированно внутри уже закрашенного силуэта, без
  собственного `clip-path`. Логика разблокировки/подписи вех и
  структура `mountainBodyGradient`/`treeGradient` не тронуты.
- Новый `test-mountain-scenery.mjs` (21 проверка: усиленная заливка +
  новое `box-shadow` у `is-today`, отличимость от `is-done`/`is-
  selected`, 6 групп покачивания с разными `animation-duration`/
  `-delay`, отключение под reduced-motion, 3 облака с `cloud-drift` +
  тоже отключение под reduced-motion, точное число новых декоративных
  элементов (камни/кусты/снежные пятна/блики), все новые элементы в
  пределах viewBox, инварианты старого контракта горы не нарушены) —
  итого 442, все проходят. `sw.js`'s `CACHE_NAME` — v5 → v6.

Правки по багрепорту на предыдущий заход (та же сессия) — чёрные точки
на горе, пустые однотонные зоны, бледный «сегодня»:
- **Чёрные точки-артефакты** — `.mountain-rock` красился в
  `--stage-rocks-dark` (`#1c211c`/`#0a0c0a` — почти-чёрный легаси-токен
  ещё с до-градиентной версии горы, по ошибке переиспользованный для
  акцентов). Заведены два новых токена в тон `--stage-rocks`
  (`variables.css`, обе темы): `--stage-rocks-pebble` (темнее,
  для камней) и `--stage-rocks-ledge` (светлее, для нового силуэта
  «уступ» — плоский скруглённый прямоугольник, разнообразит круглые
  камни). Регрессия закрыта тестом (`isNearBlack()` — сумма RGB < 60 —
  на `.mountain-rock`/`.mountain-ledge` в обеих темах).
- **Пустые зоны по всей горе, не только у тропы** — раньше все акценты
  (ёлки, камни/кусты/снег) считались через `pointAtProgress(p)` +
  маленький `dx,dy`, то есть цеплялись строго за кривую хребта; сама
  заливка горы (`.mountain-body`) — один прямоугольник на всю ширину
  400×600, так что вся площадь под тропой/справа от неё оставалась
  пустой. Новый хелпер `approxRidgeY(x)` (кусочно-линейная
  аппроксимация по уже существующему массиву `RIDGE`) + `pointBelowRidge
  (x, margin)` (плюс `RIDGE_SAFETY_MARGIN=15` — запас на отклонение
  реальной кривой Catmull-Rom от прямых отрезков) — точка на любом `x`,
  гарантированно внутри уже закрашенного силуэта, без привязки к
  тропе. На нём: **10 доп. ёлок** (`SCATTERED_TREES`, x≈15-250,
  луговая+лесная зона — итого 16 вместо 6, переиспользуют существующий
  `treeShape()` целиком — ни джиттер, ни покачивание не переписывались);
  **камни/уступы/кусты/снежные пятна/блики** расширены с 3/2/2/2 до
  6/3(новый класс)/3/3/4, разнесены по всей ширине каменной зоны
  (x≈230-395) тем же `pointBelowRidge`; новая **`textureDotsMarkup()`**
  — 26 едва заметных точек (`opacity` 0.05-0.06, светлые/тёмные
  вперемешку), координаты через детерминированный `pseudoRandom(seed)`
  (синус-хэш, не `Math.random()` — воспроизводимо и тестируемо),
  раскиданы по всей поверхности (все 4 цветовые зоны разом одним
  механизмом, не по спискам на зону).
- **«Сегодня» в календаре** — вместо полупрозрачного `rgba(accent,
  0.55/0.3)`+кольца теперь ровно `background: var(--color-accent)` +
  `var(--color-accent-contrast)` текст — байт-в-байт тот же цвет, что у
  `.today-item.is-done .today-check` (`css/components.css`). Комбо-
  правило `.is-today.is-selected` (accent-dark кольцо поверх) не
  тронуто. `is-today` и `is-done` теперь совпадают визуально — осознанно
  (это и есть «тот же зелёный для консистентности»), различаются другие
  состояния (`is-selected`/`is-future`).
- `test-mountain-scenery.mjs` переписан под новые числа/цвет (28
  проверок — было 21, +7: новый класс `.mountain-ledge`, `no near-black`
  регрессия в обеих темах, разброс ёлок/камней по X); `test-calendar-
  redesign.mjs`'s чек на «today's cell ... gradient background»
  переписан на «сплошной `--color-accent`» (тот же счёт файла).
  Итого 449, все проходят. `sw.js`'s `CACHE_NAME` — v6 → v7.

Пятый заход (та же сессия) — по факту предыдущая правка (камни/уступы/
кусты/снежные пятна/текстурный шум) не понравилась: попросили убрать
вообще все точки из зелёной И серой зон, добавить траву вместо них в
зелёных зонах, серую (каменную) зону оставить полностью чистой,
облака в небе не трогать.
- **Удалено целиком** из `js/mountainSvg.js`: `ROCK_DETAILS`/
  `LEDGE_DETAILS`/`BUSH_DETAILS`/`ROCK_SNOW_PATCH_DETAILS`/
  `SNOW_SPARKLE_DETAILS`, `bushShape()`, `mountainDetailsMarkup()`,
  `pseudoRandom()`, `TEXTURE_DOT_COUNT`, `textureDotsMarkup()` — ничего
  из этого больше никуда не вызывается. Соответствующие CSS-правила в
  `css/mountain.css` (`.mountain-rock`/`.mountain-ledge`/`.mountain-
  bush`/`.mountain-snow-patch`/`.mountain-snow-sparkle`/`.mountain-
  texture-dot--dark/--light`) тоже удалены. Токены `--stage-rocks-
  pebble`/`--stage-rocks-ledge` (`variables.css`, заводились в прошлый
  заход именно под удаляемые камни/уступы) убраны как теперь мёртвые —
  единственный потребитель исчез.
- **Облака не тронуты** — `CLOUD_POSITIONS`/`cloudShape()`/
  `cloudsMarkup()`/`@keyframes cloud-drift`/`.mountain-cloud` в обоих
  файлах остались байт-в-байт как были (они и раньше жили вне
  клипованного тела горы, в небе, геометрически никогда не были «в
  серой зоне» — то, что читалось как «облака в каменной зоне», было
  бледными `.mountain-snow-patch`, которые теперь тоже удалены).
- **Добавлена трава** — новый блок в `js/mountainSvg.js`, по прямому
  образцу уже существующих `SCATTERED_TREES`/`treeShape()`: 6 пучков
  вдоль тропы (`GRASS_TRAIL_PROGRESS`, через `pointAtProgress(p)`) + 10
  разбросанных по всей зелёной массе (`SCATTERED_GRASS`, через уже
  существующий `pointBelowRidge(x, margin)`) — итого 16, та же
  плотность, что у 16 ёлок. `grassTuft()` — тонкий 3-лепестковый веер
  (полигоны-лезвия влево/по центру/вправо от одной базовой точки),
  цвет `var(--stage-base-dark)` (уже существующий токен — «в тон, но
  темнее», как просили, без новых переменных). Своя анимация
  `@keyframes grass-sway` (±6°, короче цикл — 2с база — трава реагирует
  на ветер быстрее дерева) — та же техника `transform-box: fill-box` +
  `transform-origin: center bottom`, что и у `.mountain-tree-sway`,
  детерминированные `animation-duration`/`-delay` по индексу (не
  синхронно). Обнаружена и исправлена попутная ошибка: изначальные
  `margin` для нескольких `SCATTERED_GRASS`-точек у самого левого края
  (x=20/25/40/65) превышали реальную доступную глубину силуэта в этом
  x (у подножья горы клин очень узкий — высота внутренней области
  растёт слева направо), из-за чего эти пучки травы уезжали за нижний
  край холста (y>600); margin-ы для этих точек уменьшены до
  безопасных значений.
- `test-mountain-scenery.mjs` переписан: удалены все проверки на
  камни/уступы/кусты/снежные пятна/блики/текстурные точки, вместо них —
  регрессия «всё убрано» (0 элементов на каждый старый класс) +
  проверки на облака (без изменений, как регрессия «не тронуты») +
  новые проверки на траву (16 пучков, 3 лепестка на пучок, `grass-
  sway` keyframes, разброс `animation-duration`/`-delay`, разброс по X,
  цвет, отключение под `prefers-reduced-motion`, все элементы внутри
  viewBox) — 31 проверка (было 28, +3). `test-mountain-redesign.mjs`/
  `test-calendar-redesign.mjs` не менялись. Итого 452, все проходят.
  `sw.js`'s `CACHE_NAME` — v7 → v8.

Шестой заход (та же сессия) — предыдущая правка убрала все точки/пятна
из серой зоны, но зелёная зона осталась заметно пустой: ёлки
заканчивались на p=0.41 (x≈205), заметно не доходя до границы с серой
зоной (p=0.56, x=250), трава/ёлки были довольно редкими. Задача —
плотно наполнить зелёную зону целиком, вплоть до самой границы, плюс
добавить барашков.
- **Ёлки**: `TREE_PROGRESS` (вдоль тропы) расширен с 6 до 8 точек —
  добавлены `0.47`/`0.53`, прямо у границы леса/камня (`RIDGE`'s
  «rocks» стадия начинается на `p=0.56`). `SCATTERED_TREES`
  (разбросанные по всей зелёной массе) расширен с 10 до 20 точек, `x`
  теперь доходит до 246 (было 250 у одной точки, но с большим `margin`,
  из-за чего фактически не доставала до границы по глубине — см. ниже).
  Итого 28 ёлок вместо 16.
- **Трава**: `GRASS_TRAIL_PROGRESS` расширен с 6 до 8 (добавлены `0.44`/
  `0.5`), `SCATTERED_GRASS` — с 10 до 26 точек, тоже вплоть до `x=248`.
  Итого 34 пучка травы вместо 16.
- **Барашки** — новый элемент, `SHEEP_POSITIONS` (5 штук) +
  `sheepShape()`: минималистичный плоский силуэт (овальное тело —
  светлая заливка `#f7f5ef` + тёмная обводка `var(--color-ink)`,
  тёмная голова, две тёмные ножки-штрихи) — стандартная пиктограмма
  «чёрномордой овцы», узнаваемая даже в мелком размере. Статичны, без
  покачивания (пасущиеся овцы не колышутся на ветру, в отличие от
  травы/деревьев) — размещены через уже существующий `pointBelowRidge`,
  так что никогда не оказываются выше границы леса.
  Рендер-порядок в `buildMountainSvg()`: трава → овцы → ёлки.
- Попутно обнаружено и исправлено: у всех точек `SCATTERED_TREES`/
  `SCATTERED_GRASS` с большим `x` (ближе к границе) `margin` раньше
  выбирался без учёта реальной доступной глубины силуэта на этом `x`
  (глубина растёт слева направо — у самого подножья участок узкий) —
  из-за этого декорации у правого края фактически оказывались далеко
  **ниже** видимой линии хребта на этом `x`, а не прямо у границы, как
  требовалось. Новые списки пересчитаны так, чтобы точки с большим `x`
  сидели с небольшим отступом от самой кривой хребта — визуально прямо
  у перехода в серую зону.
- `test-mountain-scenery.mjs`: числа обновлены под 28 ёлок/34 травы,
  добавлены проверки «максимальный x у ёлок/травы ≥230» (граница
  реально достигнута, не только «шире, чем было») и новый блок на
  овец (5 штук, светлое тело+обводка, тёмная голова, 2 ноги, без
  анимации, разброс по X, в пределах viewBox) — итого 40 проверок
  (было 31, +9). Итого 461, все проходят. `sw.js`'s `CACHE_NAME` —
  v8 → v9.

Баг-фикс (та же сессия) — в «Календаре месяца» отдельной привычки
(`js/views/calendarView.js`, вертикальный список дней в
Привычки → карточка привычки) галочка выполнения не показывалась для
прошедших дней вне 3-дневного окна редактирования (`EDITABLE_WINDOW_
DAYS` в `js/logic/dateUtils.js`), хотя привычка была фактически
отмечена — воспроизведено ровно на сценарии из отчёта (сегодня
25.08, день 22 — ровно на границе 3-дневного окна).
- **Причина**: `dayPillHtml()`'s `const ringState = locked ? "locked"
  : done ? "done" : "empty";` — если день вне окна редактирования
  (`locked === true`), состояние `"locked"` подставлялось безусловно,
  даже если `done === true`, и до проверки `done` дело просто не
  доходило. Порядок проверки поменян на `done ? "done" : locked ?
  "locked" : "empty"` — «нельзя редактировать» и «выполнено» теперь
  независимые флаги, как и должно быть (просмотр статуса vs
  возможность редактирования — разные вещи).
- Ограничение на редактирование (`disabled`/`is-locked`,
  клик не открывает `dayDetailView`) не тронуто — только починена
  логика ВЫБОРА состояния кольца-индикатора для отображения.
- **Стилистика**: `statusRing()` (`js/illustrations.js`) получил новый
  параметр `muted` — для состояния `"done"` добавляет класс
  `status-ring--muted`, стили которого (`css/illustrations.css`) дают
  ту же зелёную заливку, но полупрозрачную (`rgba(var(--color-accent-
  rgb), 0.35)`), и более тёмную (`--color-accent-dark`) галочку внутри
  для контраста — визуально бледнее, но не «выключенный» серый.
  `calendarView.js` передаёт `muted: done && !cell.isToday` — то есть
  ЛЮБОЙ выполненный день, кроме сегодняшнего (независимо от того,
  редактируемый он ещё или уже нет), получает приглушённый оттенок;
  сегодняшний остаётся насыщенным, как раньше. `habitsView.js`'s
  собственный вызов `statusRing()` (бейдж на карточке привычки,
  всегда про «сегодня») параметр не передаёт — не затронут, поведение
  прежнее.
  Новый класс `.day-pill.is-done` — раньше `.day-pill.is-locked`
  затемняла ВЕСЬ пилл до `opacity: 0.62` независимо от того, есть ли
  там на самом деле данные; теперь `.day-pill.is-locked.is-done`
  возвращает `opacity: 1` — заблокированный-но-выполненный день не
  тонет визуально (приглушённость самого кольца уже достаточно несёт
  смысл «прошлый день»).
- Новый `test-daypill-status-bug.mjs` (18 проверок): прямое
  воспроизведение бага (запись за день вне окна редактирования всё
  ещё показывает `is-locked`+`disabled`, но теперь и `is-done` +
  `status-ring--done`+`--muted`, галочка реально рисуется, сам пилл не
  затемнён), тот же приглушённый вид у выполненного «вчера» (в окне
  редактирования, но не сегодня), сегодняшний выполненный день
  остаётся НЕ приглушённым, разные цвета заливки колец у приглушённого
  и яркого состояний, регрессия — по-настоящему невыполненный
  заблокированный день по-прежнему показывает `status-ring--locked`,
  бейдж на карточке в списке привычек не затронут. `test-editable-
  window.mjs` (ограничение на редактирование) не менялся, все 10
  проверок по-прежнему проходят. Итого 479, все проходят. `sw.js`'s
  `CACHE_NAME` — v9 → v10.

Седьмой заход (та же сессия) — жалоба «ёлки стоят слишком плотно/
скученно... особенно в верхней зелёной зоне» + «слева от массива ёлок
и тропы пустая зелёная область». Проверил текущий рендер напрямую
(скриншот `.mountain-wrap`) — причина оказалась структурной: деревья/
трава размещались ДВУМЯ независимыми списками (`TREE_PROGRESS`/
`GRASS_TRAIL_PROGRESS` — точки прямо на кривой хребта, и
`SCATTERED_TREES`/`SCATTERED_GRASS` — со смещением `margin`), которые
три захода подряд независимо разрастались «чтобы дотянуть до
границы» — оба списка почти всегда клали точки с маленьким `margin`,
то есть плотной лентой вдоль диагонали тропы, а вся остальная (и
самая большая) площадь силуэта — заливка вглубь от хребта, вплоть до
y=600, доступная глубина растёт от считаных пикселей у x≈0 до 300+ у
x≈248 — оставалась совсем нетронутой.
- **Заменил оба списка одним детерминированным алгоритмом** —
  `jitteredGreenGrid({ cols, rows, marginStartFrac, marginEndFrac,
  seed })` (новая функция в `js/mountainSvg.js`, использует уже
  существующие `approxRidgeY(x)`/`RIDGE_SAFETY_MARGIN`): сетка `cols`
  колонок по x, `rows` точек по глубине НА КОЛОНКУ, глубина каждой
  колонки берётся от её РЕАЛЬНОЙ доступной глубины
  (`583 - approxRidgeY(x)`), так что сетка сама сужается у тонкого
  левого края и расширяется у широкого правого — без ручного подбора
  `margin` под каждую точку (источник всех трёх прошлых багов с этим
  списком). Каждая точка сдвинута детерминированным псевдослучайным
  хэшем `hash(seed)` (`Math.sin(seed*12.9898)*43758.5453`, дробная
  часть — тот же приём, что уже использовался для убранной текстуры и
  джиттера стволов) — не жёсткая решётка, но и не куча/пустота.
- **Деревья**: `TREE_PROGRESS`+`SCATTERED_TREES` удалены целиком,
  `treesMarkup()` → `jitteredGreenGrid({ cols: 8, rows: 4,
  marginStartFrac: 0.03, marginEndFrac: 0.85, seed: 1 })` — 32 дерева
  (было 28, но теперь на ~3× большей площади — плотность у самого
  хребта заметно ниже, «куча» ушла, а прежде пустой глубокий интерьер
  реально заполнен, не просто «задет»).
- **Трава**: `GRASS_TRAIL_PROGRESS`+`SCATTERED_GRASS` удалены,
  `grassMarkup()` → `jitteredGreenGrid({ cols: 10, rows: 5,
  marginStartFrac: 0.01, marginEndFrac: 0.92, seed: 101 })` — 50
  пучков (было 34); другой `seed`, чем у деревьев, чтобы сетки не
  совпадали по фазе.
- **Барашки** не тронуты — на их плотность жалоб не было.
  `treeShape()`/`grassInstance()` (сам силуэт, джиттер ствола,
  sway-тайминг) не менялись — только источник точек `(x, y)`.
  Числа `cols`/`rows`/`marginEndFrac` подобраны по скриншоту (2
  итерации: первая версия — 21 дерево/36 травы — оказалась заметно
  реже, чем требовалось, «пустая площадь» читалась по-прежнему;
  добавлено до 32/50).
- `test-mountain-scenery.mjs`: числа обновлены (32/50), добавлена
  проверка разброса ПО ГЛУБИНЕ от хребта (не только по x — прежние
  проверки видели только x-разброс и не поймали бы саму жалобу «куча
  у тропы») — локальная копия `RIDGE`/`approxRidgeY()` в тесте (не
  экспортированы из модуля, тест меряет геометрию независимо) — итого
  43 проверки (было 40, +3). Итого 482, все проходят. `sw.js`'s
  `CACHE_NAME` — v10 → v11.

Восьмой заход (та же сессия) — жалоба «деревья справа/в основном
массиве вдоль и правее тропы слишком густо, накладываются друг на
друга — «каша»; левая часть у самого края почти пустая». Прошлый заход
(седьмой) не полностью устранил причину: `jitteredGreenGrid()` клала
ФИКСИРОВАННОЕ число `rows` точек в КАЖДУЮ из `cols` РАВНЫХ по x
колонок — но реальная доступная глубина силуэта (`583 -
approxRidgeY(x)`) отличается почти в 60 раз между самым тонким левым
краем (≈5px у x=10) и самым широким правым (≈320px у x=248). Одно и то
же число точек на колонку при таком разбросе площади даёт крайне
неравномерную ПЛОТНОСТЬ: у тонкого края точки схлопываются почти в одну
(на глаз — «пусто», не различить слипшиеся деревья), в средних колонках
— тесно (кроны накладываются, «каша»), у широкого края — по факту
реже всего.
- **`jitteredGreenGrid()` заменена на `densityGrid()`** — число точек
  на колонку больше не константа, а `Math.round((colWidth * avail) /
  targetAreaPerItem)`, то есть пропорционально ПЛОЩАДИ конкретной
  колонки при фиксированной целевой плотности (`targetAreaPerItem`,
  кв.px на одну точку); слишком тонкие колонки естественно получают 0
  точек (`if (rows <= 0) continue`) вместо утрамбовки.
- **Численная проверка вскрыла остаточное наложение**: даже с
  density-grid, ближайший сосед у части деревьев оказывался в 6.4px
  (кроны до 35-40px шириной — видимое наложение), потому что соседние
  колонки джиттерятся независимо друг от друга — ничего не мешает
  точке одной колонки оказаться прямо рядом с точкой соседней. Добавлен
  `enforceMinSpacing(points, minDist)` — жадный пост-процесс (в порядке
  генерации отбрасывает точки ближе `minDist` px к уже принятой).
- **Калибровка** (отдельный Node.js прогон той же формулы, до правки
  исходника): для деревьев `targetAreaPerItem: 1080` + `minDist: 28`
  (первая попытка) среза́л слишком много — из 29 кандидатов оставалось
  17, экран снова читался как «пусто». Перебор пар
  `(targetAreaPerItem, minDist)` дал `550/24` — 59 кандидатов → 27
  финальных дерева, мин. расстояние 24.6px, среднее 28.7px (без
  видимых наложений, без чрезмерной редкости). Для травы текущий
  `targetAreaPerItem: 540` + `minDist: 10` почти не отбраковывает
  кандидатов (61 → 59) — пересчёт не потребовался.
- **Деревья**: `TREE_GRID.targetAreaPerItem` 1080 → 550,
  `enforceMinSpacing(..., 28)` → `enforceMinSpacing(..., 24)` в
  `treesMarkup()` — 32 → 27 дерева.
- **Трава**: `GRASS_GRID`/`grassMarkup()` без изменений по сути (уже
  использовали `densityGrid`/`enforceMinSpacing` с прошлого захода),
  но итоговое число сместилось 50 → 59 из-за замены самой формулы
  плотности на `densityGrid()`.
- Подтверждено скриншотами (светлая и тёмная тема): у тонкого левого
  кончика силуэта теперь видны 1-2 дерева + трава + барашек вместо
  пустоты, в основном массиве деревья стоят раздельно, без наложения
  крон, серая зона по-прежнему чистая. Барашки и облака не трогались.
- `test-mountain-scenery.mjs`: числа обновлены (27 деревьев/59
  травы), добавлены 2 новые проверки — минимальное расстояние до
  ближайшего соседа у КАЖДОГО дерева (≥15px — прямая регрессия на
  «наложение/кашу», а не только на общий разброс x/глубины) и наличие
  хотя бы одного дерева у самого тонкого левого края (x<70) — прямая
  регрессия на «пустая левая часть». Итого 45 проверок (было 43, +2).
  Итого 484 (полный набор), все проходят. `sw.js`'s `CACHE_NAME` — v11
  → v12.

Девятый заход (та же сессия) — жалоба «правая часть горы (за пределами
тропы, широкая область вправо) почти полностью пустая» + скриншот
(сплошной зелёный градиент без единой детали). Проверил напрямую —
причина оказалась глубже, чем просто «мало деревьев»: заливка горы
(`mountainBodyGradient`) — ОДИН линейный градиент по Y (grass→forest→
rock→snow), одинаковый для ВСЕХ x, а низ силуэта всегда `y=600` по
всей ширине (`L400,600 L0,600 Z`). То есть на любом x от 0 до ~400
нижняя часть столбца закрашена травяным цветом — независимо от того,
что хребет (`RIDGE`) в этой точке уже помечен как stage `"rocks"`/
`"summit"`. Деревья/трава были ограничены `GREEN_ZONE_MAX_X = 248`
(x, где сам ХРЕБЕТ входит в скалистую стадию) — но это не граница, где
заканчивается зелёная ЗАЛИВКА: она продолжается горизонтальной полосой
почти на всю ширину. Кроп `mountain-bottom-strip.png` (нижняя полоса
на всю ширину) подтвердил: слева деревья, справа — сплошной пустой
зелёный, в точности как на скриншоте пользователя. Численно: площадь
уже засаженной зоны (x=10..248) ≈ 32 300 кв.px, площадь ранее
незасаженной полосы (x=248..390) ≈ 44 400 кв.px — пустая часть БОЛЬШЕ
всего существующего массива.
- **x-отсечка заменена на Y-отсечку по цвету.** Новая константа
  `VEGETATION_MIN_Y = 270` (граница forest→rock в градиенте, offset
  55% → `y = 600 * (1 - 0.55)`). Доступная глубина колонки теперь
  считается от `max(approxRidgeY(x) + RIDGE_SAFETY_MARGIN,
  VEGETATION_MIN_Y)` до `583`, а не всегда от самого хребта — там, где
  хребет уже высоко (узкий x у правого края), верхняя граница зоны
  больше не «уезжает» вместе с хребтом вверх, а держится на уровне
  реальной цветовой границы. Это даёт на широких правых колонках
  примерно одинаковую доступную глубину (~313px) вместо
  неограниченно растущей.
- **x-диапазон расширен** с `[10, 248]` до `[10, 390]` (390 — тот же
  отступ 10px от правого края viewBox, что уже использовался слева).
- **`densityGrid()` теперь возвращает `{x, y}` напрямую**, а не `{x,
  margin}` — `margin` был смещением от хребта и терял смысл, когда
  верхняя граница может быть либо хребтом, либо `VEGETATION_MIN_Y`.
  `pointBelowRidge()` для деревьев/травы больше не нужен —
  `treesMarkup()`/`grassMarkup()` используют точки напрямую; функция
  осталась только для барашков (`sheepMarkup()`), которых не трогали.
- **Пересчитаны `cols`/`targetAreaPerItem`** под вдвое большую площадь
  (тот же перебор, что и в прошлый заход, пороги `enforceMinSpacing`
  — 24px деревья/10px трава — не менялись): деревья `cols: 16→26,
  targetAreaPerItem: 550` (без изменений) → 27→60 итоговых дерева;
  трава `cols: 20→32, targetAreaPerItem: 540→480` → 59→148 итоговых
  пучков.
- Подтверждено скриншотами (светлая и тёмная тема) и кропом ранее
  пустой зоны: широкая пустая полоса теперь заполнена деревьями и
  травой на том же уровне плотности, что и остальная зелёная площадь,
  без шва на старой границе x=248. Барашки и облака не трогались.
- `test-mountain-scenery.mjs`: числа обновлены (60 деревьев/148
  травы), x-range проверки подняты под новый правый край (~x=385
  вместо ~220), добавлены 2 новые проверки — доля деревьев/травы с
  x>260 заметно больше нуля (прямая регрессия на «пустая правая
  часть»); порог ближайшего соседа у деревьев чуть смягчён (15→12px —
  на большем числе деревьев чаще встречается законный
  `treeShape()`-джиттер до ±5px поверх минимального расстояния сетки,
  всё ещё далеко от прежних ~6px реального наложения). Итого 47
  проверок (было 45, +2). Итого 486 (полный набор), все проходят.
  `sw.js`'s `CACHE_NAME` — v12 → v13.

Десятый заход (та же сессия) — «деревьев на горе стало слишком много,
выглядит перегруженно, уменьшить количество примерно в 2 раза,
равномерно по всей площади; траву/барашков/анимации не трогать».
Прогнал тот же вид калибровочного перебора `densityGrid`+
`enforceMinSpacing`, что и в прошлые заходы, но на этот раз с целью не
«убрать пустоты», а именно «уменьшить количество вдвое, не потеряв
равномерность»: поднял `TREE_GRID.targetAreaPerItem` (целевую площадь
на одно дерево, т.е. напрямую снизил плотность решётки) и соразмерно
`enforceMinSpacing`'s `minDist` (иначе после разрежения решётки порог
наложения остался бы прежним, слишком мягким для нового рисунка).
`cols` (26, пространственное разрешение решётки по x) не трогал — само
распределение по ширине осталось тем же, изменилась только доля
выживающих точек.
- **Деревья**: `TREE_GRID.targetAreaPerItem` 550 → 1000,
  `enforceMinSpacing(..., 24)` → `enforceMinSpacing(..., 34)` в
  `treesMarkup()` — 60 → 30 дерева (ровно вдвое, перебор пар
  `(targetAreaPerItem, minDist)` подтвердил именно эту пару даёт точное
  число), минимальное расстояние между соседями выросло с ~24px до
  ~28-34px (пропорционально возросшей площади на дерево, без
  наложения).
- **Трава/барашки/облака не трогались** — ни `GRASS_GRID`, ни
  `grassMarkup()`, ни `SHEEP_POSITIONS`/`sheepMarkup()`, ни
  `CLOUD_POSITIONS`/`cloudsMarkup()` не менялись (явное требование).
- **Принятый побочный эффект**: у самого тонкого левого края (x≈10-70,
  физически помещается максимум 1 дерево — глубина силуэта там всего
  0-29px) при вдвое меньшей плотности этот единственный слот теперь не
  округляется даже до одного дерева — самое левое дерево сместилось с
  x≈58 до x≈69-74. Это не «пустая зона» в смысле прошлых багов (там
  были десятки тысяч кв.px совсем без единой детали) — это потеря
  ОДНОГО дерева на объективно крошечном пятачке, где и раньше стояло
  только одно; более плотная альтернатива сохраняла бы этот край, но
  давала уменьшение лишь на ~37%, а не вдвое, как прямо попросил
  пользователь.
- Подтверждено скриншотами (светлая и тёмная тема): деревьев заметно
  меньше, сцена не выглядит перегруженной, распределение по ширине
  по-прежнему равномерное (включая расширенную в прошлый раз правую
  часть), без новых крупных пустых пятен и без наложения крон; трава и
  барашки визуально не изменились.
- `test-mountain-scenery.mjs`: число деревьев 60 → 30; порог
  ближайшего соседа поднят (12→20px, под новый, более разреженный
  minDist=34); порог «левый край x<70» ослаблен до `x<100` с
  комментарием про принятый компромисс (см. выше — это потеря одного
  дерева на пятачке, а не регрессия старого бага); порог покрытия
  правой полосы (`x>260`) снижен пропорционально числу деревьев
  (10→5). Проверки по траве/барашкам/облакам не менялись. Итого те же
  47 проверок (только пороги и описания). Итого 486 (полный набор),
  все проходят. `sw.js`'s `CACHE_NAME` — v13 → v14.

Одиннадцатый заход (та же сессия) — «доработать барашков: детализация,
размер, расстановка, анимация выпаса, интерактивность» (5 пунктов).
Прочитал текущий код (`SHEEP_POSITIONS`/`sheepShape()`/`sheepMarkup()`
в `js/mountainSvg.js`, `css/mountain.css`, `js/views/mountainView.js`),
прогнал дизайн через Explore- и Plan-агентов, ключевая находка —
**порядок отрисовки в `buildMountainSvg()`** был прямой причиной
«деревья перекрывают барашков»: `${grassMarkup()}${sheepMarkup()}
${treesMarkup()}` рисовало барашков ДО деревьев.
- **Детализация**: тушка-эллипса заменена на **6 перекрывающихся
  кругов** (`.mountain-sheep-wool`, тот же приём, что уже даёт
  `cloudShape()` для облака — 3 эллипсы внахлёст) — контур сам читается
  как кучерявая шерсть, без кривых/path-математики. Голова крупнее
  (r 2.6→7.5), внутри — 2 бледных кружка-глаза
  (`.mountain-sheep-eye`), бледная мордочка-эллипса
  (`.mountain-sheep-muzzle`), 2 тёмных ушка (`.mountain-sheep-ear`).
  Ножек 2→4 (`.mountain-sheep-leg`) — при укрупнении и «пушистом»
  силуэте 2 тонкие ножки смотрелись бы шатко. Порядок отрисовки внутри
  группы — ножки, потом шерсть, потом голова — чтобы верх ножек прятался
  под шерстью (как раньше прятался под телом-эллипсой), а не «протыкал»
  её понизу.
- **Размер**: габарит вырос с ~19×17 до ~46×31 юнитов (~2.5×), обводы
  растут непропорционально (шерсть 1→1.4px, ножки 1.2→2.2px), чтобы не
  выглядело тяжеловесно.
- **Расстановка + фикс перекрытия**: `${sheepMarkup()}` переставлен
  ПОСЛЕ `${treesMarkup()}` — барашки теперь последний декоративный
  слой, дерево не может их закрыть. `SHEEP_POSITIONS` пересчитаны на
  всю ширину `x=10..390` (раньше 40-230) — `{30,95,175,260,365}`, две
  точки (260, 365) специально попадают в зону, где включается кламп по
  `VEGETATION_MIN_Y`. Сам кламп (`Math.max(approxRidgeY(x)+
  RIDGE_SAFETY_MARGIN, VEGETATION_MIN_Y)`, тот же, что уже использует
  `densityGrid()` для деревьев/травы) добавлен прямо в
  `pointBelowRidge()` — барашки единственный вызывающий этой функции,
  так что кламп добавлен туда, а не в отдельную copy-paste функцию.
- **Анимация выпаса**: новый keyframe `sheep-graze` в `css/mountain.css`
  на `.mountain-sheep-graze` (обёртка головы, НЕ вся группа
  `.mountain-sheep` — важно, чтобы тап-эффект ниже мог отдельно
  анимировать всю группу) — короткий наклон-и-задержка ближе к концу
  цикла (не непрерывное покачивание), тайминг на барашка (по index, тот
  же приём отрицательного delay, что у `tree-sway`/`grass-sway`) — 5
  разных duration/delay, ни одной синхронной пары. `+prefers-reduced-
  motion` блок, тот же паттерн, что у остальных анимаций.
- **Тап-эффект**: лёгкий подпрыг (`sheep-hop`, вся группа) + сердечко,
  всплывающее у головы и тающее (`sheep-heart-pop`, новый маркап-хелпер
  `sheepHeartPopMarkup(x,y)` в `mountainSvg.js` — 2 круга + треугольная
  «капля»). Барашек **никогда не удаляется из DOM** — важно, потому что
  `patchMountainView()` никогда не перестраивает декоративную разметку,
  значит удалённый барашек не вернулся бы до полного ремаунта; тап
  только добавляет/убирает временный CSS-класс и временный дочерний
  узел. Один делегированный `click` на `.mountain-svg`, повешенный один
  раз в `renderFreshMountainView()` (`wireMountainSheep()`/
  `triggerSheepTap()` в `mountainView.js`) — переживает все патчи.
  Позиция сердечка — через `data-heart-x`/`data-heart-y` на самой
  группе `<g class="mountain-sheep">` (у неё нет своего transform, все
  дочерние фигуры уже в абсолютных координатах), сердечко оборачивается
  в статичный `<g transform="translate(...)">` СНАРУЖИ анимируемой
  группы `.mountain-sheep-heart-pop` — иначе transform-атрибут и
  CSS-анимация transform на одном элементе конфликтовали бы (тот же
  подвох, что уже решён для `-sway`-обёрток деревьев/травы).
  `prefers-reduced-motion`: подпрыг и всплытие отключены, но сама
  логика очистки (`setTimeout` как страховка на случай, если
  `animationend` не сработает при `animation:none`) — не меняется.
- `test-mountain-scenery.mjs`: раздел про барашков переписан почти
  целиком — 6 шерстяных комков, 2 глаза, 1 мордочка, 2 уха, 4 ноги,
  габарит заметно больше прежнего, разброс по x > 300 (было > 100);
  новая регрессия на «деревья перекрывают» — все `.mountain-sheep` идут
  в DOM-порядке ПОСЛЕ последнего `.mountain-tree-sway`; проверка
  `sheep-graze`-анимации (имя, вариативность длительности/задержки);
  новый блок тап-теста (клик → `.mountain-sheep-hop`/
  `.mountain-sheep-heart-pop` появляются → через ~1с оба пропадают →
  барашек по-прежнему в DOM). Итого 19 проверок в разделе про барашков
  (было 6). Итого 499 (полный набор), все проходят. `sw.js`'s
  `CACHE_NAME` — v14 → v15.

Двенадцатый заход (та же сессия) — «добавить водопад и летающих птиц в
серую зону горы». Заметка для истории: в пятом заходе этой же сессии
серую/каменную зону намеренно полностью очистили от любого декора по
прямой просьбе пользователя — сейчас пользователь явно просит
обратное, для именно этих двух новых элементов, так что выполнено
напрямую, без уточнений, это не конфликт, а осознанная новая просьба.
- **Цвет каменной зоны — абсолютная Y-полоса, не зависит от x** (та же
  логика, что уже задокументирована в `VEGETATION_MIN_Y` для зелёной
  зоны): `mountainBodyGradient` идёт по Y от 0% (низ, y=600) до 100%
  (верх, y=0); rock-стоп — offset 68-85%, то есть `y=192..90`.
  Координаты водопада/птиц подобраны и численно проверены по
  `approxRidgeY()` под эту полосу.
- **Водопад** — `waterfallMarkup()`, один `<path>` с волнистым `d`
  (несколько `Q`-сегментов, чередующих изгиб) — рендерится ВНУТРИ уже
  существующей клипованной группы тела горы (сразу после
  `<rect class="mountain-body">`), а не через ручной подбор координат,
  как трава/деревья/барашки — граница силуэта сама гарантирует, что
  линия не вылезет за пределы горы. Новый `<linearGradient
  id="waterfallGradient">` в `<defs>` — сплошной цвет сверху, падающая
  `stop-opacity` к низу (имитация водяной пыли/тумана, а не резкий
  обрыв линии). CSS: `stroke-dasharray` + анимация
  `stroke-dashoffset` (`waterfall-flow`, 1.8s linear infinite) —
  стандартный SVG-приём «бегущей» линии для эффекта струящейся воды;
  `drop-shadow`-свечение (голубоватое) — тот же приём, что уже даёт
  золотое свечение `.mountain-trail-walked`, просто свой цвет, для
  «поблёскивания».
- **Птицы** — `BIRD_POSITIONS` (3 штуки, середина запрошенного
  диапазона 2-4), `birdShape()` — минималистичный двойной-дуга
  «чайка»-силуэт одним `<path>` (только обвод, без заливки).
  Рендерятся ДО клипованной группы тела горы (как облака) — не
  клипованы: если бы летели внутри клипа, при выходе анимации за
  пределы силуэта они бы обрезались серединой полёта. Каждая позиция
  вручную проверена по `approxRidgeY(x)` на ВСЕХ крайних точках своей
  анимации (не только базовой позиции) — с запасом ~15px, тем же, что
  использует `RIDGE_SAFETY_MARGIN` — так нашёлся и исправлен реальный
  просчёт: третья птица с исходными координатами (265,190) в одном из
  кадров анимации проваливалась НИЖЕ хребта (клиренс −26px) — её базу
  сдвинули на (230,175), после чего клиренс везде положительный.
  Цвет — `var(--color-ink)` (тот же токен, что у головы барашка) —
  сам инвертируется тёмная/светлая тема, то есть птицы остаются
  контрастными на фоне неба в обеих темах без дополнительных правил.
  2 варианта дуги полёта (`bird-fly-a`/`bird-fly-b`, CSS keyframes,
  `transform: translate(...)` в несколько точек) + свои
  duration/delay на птицу (тот же детерминированный приём
  отрицательного delay, что у деревьев/травы/барашков) — ни одна не
  летит синхронно с другой. Силуэт птицы сам симметричен слева-направо
  — отдельного «зеркалирования» под третью птицу не делалось (не дало
  бы видимой разницы), различие только в варианте дуги/тайминге/
  масштабе/позиции.
- И то, и другое отключается под `prefers-reduced-motion` (тот же
  паттерн, что у всех остальных анимаций горы).
- Подтверждено скриншотами (светлая и тёмная тема) + числовой
  проверкой смещения `stroke-dashoffset` в двух разных кадрах (вода
  реально «течёт», а не статична).
- `test-mountain-scenery.mjs`: новый раздел (водопад — 1 path, dash-
  паттерн, имя анимации, реальное смещение dashoffset со временем,
  Y-диапазон внутри каменной полосы; птицы — 2-4 штуки, оба варианта
  keyframe встречаются, вариативность duration/delay, регрессия на
  «не внутри клипа» — все `.mountain-bird-fly` вне группы с
  `clip-path`; оба отключаются под `prefers-reduced-motion`; отсутствие
  пересечения классов с существующим декором). Итого 73 проверки (было
  60, +13). Итого 512 (полный набор), все проходят. `sw.js`'s
  `CACHE_NAME` — v15 → v16.

Тринадцатый заход (та же сессия) — «пересоздать барашков в виде
красивых SVG-иллюстраций» по референсу (пушистое кучерявое тело с
полутонами, крупные глаза с бликом, розовые уши и нос-сердечко, тёмная
мордочка/ноги, голова слегка повёрнута), с сохранением размера/
расстановки/тап-эффекта, и с 5 РАЗНЫМИ анимациями «жизни» (по одной на
барашка, не синхронно).
- **«Переиспользуемый компонент» — решил как JS-функцию
  (`sheepShape()`), не буквальный SVG `<symbol>`+`<use>`.** Три из
  пяти барашков должны анимировать РАЗНЫЕ внутренние части (у одного —
  голова, у другого — только глаза, у третьего — всё тело, у
  четвёртого — только уши); при `<use>` все клоны одного `<symbol>`
  получают одинаковые CSS-классы одинаково — нет чистого способа
  анимировать ровно одну часть у ровно одного клона без хрупких трюков
  с CSS-переменными сквозь shadow-дерево. JS-функция, вызванная 5 раз
  с разными параметрами — тот же принцип «один источник правды для
  формы», уже используемый везде в файле (`treeShape()`,
  `grassTuft()`, `cloudShape()`).
- **Шерсть**: `WOOL_LUMPS` 6 → 9 комков (тот же общий охват силуэта,
  плотнее «облако»), новый `<radialGradient id="sheepWoolGradient">`
  (кремовый центр → чуть тёмнее по краю) — все `.mountain-sheep-wool`
  красятся этим градиентом вместо плоского цвета — «лёгкие тени/
  полутона внутри кудряшек» без отдельного слоя теней.
- **Морда**: фиксированный тёмный цвет (`#332f36`, буквальный hex, НЕ
  `var(--color-ink)`, который специально инвертируется тёмная/светлая
  тема — для мордочки животного это неверно, она не должна белеть
  ночью; проверено численно, что цвет головы идентичен в обеих темах).
  Глаза — теперь 3 слоя на глаз (склера крупная бледная + зрачок тёмный,
  смещённый в сторону `facing` + крошечный блик светлым, смещён
  внутрь зрачка вверх-влево) вместо одного бледного кружка. Уши —
  двухслойные (тёмная внешняя эллипса + розовая внутренняя), обе в
  одной `rotate()`-обёртке на ухо. Мордочка-пятно убрана целиком,
  вместо неё розовый нос-сердечко (тот же визуальный приём — 2 кружка
  + треугольник, — что уже даёт `sheepHeartPopMarkup()` для
  тап-эффекта, отдельный маленький инлайновый маркап, не общая
  функция — разный масштаб/роль). Голова стала эллипсом (не кругом) со
  статичным `transform="rotate(±11 cx cy)"`-атрибутом — поворот круга
  был бы незаметен, эллипса даёт «слегка повёрнута/наклонена».
- **Единая структура, разные анимации**: markup каждого барашка
  ВСЕГДА содержит все обёртки (`.mountain-sheep-graze`,
  `.mountain-sheep-eyes`, `.mountain-sheep-body-sway`,
  `.mountain-sheep-ear` ×2) — но реальный `animation` в CSS живёт
  только на `--active`-модификаторе, который JS добавляет только
  нужному барашку (`SHEEP_LIFE_ANIM`, индексируется по номеру барашка,
  не хранится в `SHEEP_POSITIONS` — свойство «какой это барашек», тот
  же дух, что у `treeShape()`'s индекс-driven варианта/таймингов):
  0=жуёт траву, 1=моргает, 2=покачивает телом, 3=дёргает обоими
  ушами, 4=моргает+дёргает ушами одновременно (комбинация из двух
  «маленьких» жестов, не из двух больших, чтобы не смотрелось
  перегружено). `.mountain-sheep-body-sway` обёрнута ВНУТРИ
  `.mountain-sheep` (не вместо неё) — иначе покачивание спорило бы с
  тап-хлопком за `transform` одного и того же внешнего элемента; та же
  задача решена и для ушей (статичный наклон — снаружи атрибутом,
  подёргивание — CSS-анимация на вложенной группе). У каждой активной
  анимации свои `duration`/`delay` (детерминированный приём
  отрицательного delay по индексу, тот же, что у деревьев/травы) — ни
  одна пара одинаковых анимаций (2 моргающих, 2 дёргающих ушами) не
  совпадает по фазе; у пятого (комбо) барашка два его эффекта тоже не
  синхронны друг с другом. Все 4 типа анимации отключаются под
  `prefers-reduced-motion`.
- **Тап-интерактивность не трогалась** — `wireMountainSheep()`/
  `triggerSheepTap()` (`js/views/mountainView.js`),
  `.mountain-sheep-hop`/`.mountain-sheep-heart-pop`
  (`css/mountain.css`), `sheepHeartPopMarkup()` — без изменений,
  подтверждено тем же тестом, что и раньше (тап всё ещё работает на
  новой разметке).
- **Размер/расстановка не менялись** — `SHEEP_POSITIONS`, кламп по
  `VEGETATION_MIN_Y`, порядок отрисовки после деревьев — как есть.
- `test-mountain-scenery.mjs`: раздел про барашков переписан почти
  целиком — 9 комков шерсти, склера/зрачок/блик по 2 на барашка,
  нос-сердечко (3 части), уши двухслойные (4 части), регрессия «лицо
  не белеет ночью» (сравнение computed-цвета с `[data-theme="dark"]`
  форсированным и без), 5 новых проверок на распределение анимаций
  (ровно 1 жуёт, ровно 2 моргают, ровно 1 покачивается, ровно 2 дёргают
  ушами, ровно 1 комбо — с проверкой рассинхронизации внутри каждой
  группы), 4 новых `prefers-reduced-motion`-проверки (по одной на тип
  анимации). Итого 40 проверок в разделе про барашков (было 30 после
  прошлого захода). Итого 522 (полный набор), все проходят. `sw.js`'s
  `CACHE_NAME` — v16 → v17.

Четырнадцатый заход (та же сессия) — «5 птиц вместо текущего
количества, убрать водопад, заменить на другое». Прочитал текущий код
(`BIRD_POSITIONS`, `waterfallMarkup()` в `js/mountainSvg.js`,
`.mountain-bird*`/`.mountain-waterfall*` в `css/mountain.css`).
- **Птицы**: `BIRD_POSITIONS` 3 → 5. Новые 2 позиции подобраны и
  проверены той же численной проверкой, что и в прошлый раз (клиренс
  от хребта на ВСЕХ крайних точках анимации, не только базовой) — с
  первой попытки 4-я птица (x≈340, у самой вершины) провалила проверку
  (хребет там уже высокий, недостаточно неба), подобрали (300,140)
  вместо этого. Оба новых duration/delay отличаются от всех
  существующих. `birdShape()`/`birdsMarkup()` не менялись.
- **Водопад убран целиком**: `waterfallMarkup()`, вызов в
  `buildMountainSvg()`, `<linearGradient id="waterfallGradient">` в
  `<defs>`, `.mountain-waterfall`, `@keyframes waterfall-flow`,
  `.mountain-gradient-stop--waterfall-top/-bottom` — всё удалено.
- **Замена — каменный тур (cairn), маленькая стопка камней**: выбрал
  сам (пользователь дал свободу выбора) — тематически это классический
  маркер тропы в пешем туризме, ложится на уже существующую метафору
  приложения (тропа/вехи-следы/флаг на вершине). `cairnMarkup()` — 3
  уплощённых эллипсы-камня внахлёст (шире внизу → уже к вершине
  стопки, с лёгким горизонтальным смещением каждого — «неровно, но
  устойчиво», как настоящий тур), тот же приём «несколько похожих фигур
  внахлёст», что уже используют `cloudShape()`/`WOOL_LUMPS`. Рендерится
  на том же месте разметки, где раньше был водопад — внутри клипованной
  группы тела горы (граница силуэта сама гарантирует непревышение
  границ). Координата проверена по `approxRidgeY()` — внутри силуэта с
  запасом ~46px, внутри серой Y-полосы. Цвет — фиксированный тёплый
  камень (`#b0a495`/`#4d453c`, буквальный hex, не `var(--color-ink)` и
  не токены самой горы `--stage-rocks*`) — тот же принцип «у объекта
  свой фиксированный цвет», уже применённый к барашкам/птицам, чтобы
  тур не сливался с холодной серо-голубой заливкой горы в обеих темах.
  **Без анимации — сознательно**: неба и так уже 5 независимо
  анимированных птиц, вторая движущаяся деталь тут была бы лишней
  («не перегружая композицию»).
- Подтверждено скриншотами (светлая и тёмная тема): ровно 5 птиц на
  разных дугах, водопада нигде нет, вместо него виден тур, не наезжает
  на тропу/подписи вех, хорошо контрастирует в обеих темах.
- `test-mountain-scenery.mjs`: весь раздел про водопад заменён на
  раздел про тур (3 камня, заливка/обвод, статичность, Y-диапазон
  внутри серой полосы, регрессия «внутри клипованной группы» —
  аналог того, что раньше проверялось для водопада); птиц теперь ровно
  5 (было «2-4»), проверки durations/delays подняты под 5 инстансов;
  проверка «водопад полностью удалён» (0 узлов
  `.mountain-waterfall`); поправлена общая проверка границ viewBox и
  «не пересекается по классам с другим декором»
  (`.mountain-waterfall` → `.mountain-cairn-stone`). Итого те же 83
  проверки в объединённом разделе (структура другая, число сопоставимо
  — было 73 после захода с водопадом+3 птицами). Итого 522 (полный
  набор), все проходят. `sw.js`'s `CACHE_NAME` — v17 → v18.

Пятнадцатый заход (та же сессия) — «больше облаков ниже, убрать камни,
добавить туман». Пользователь явно называет добавленный в прошлый
заход тур «камнями/валунами» — не понравился, убираем полностью.
Прочитал текущий код (`CLOUD_POSITIONS`, `cairnMarkup()`,
соответствующий CSS).
- **Облака**: `CLOUD_POSITIONS` 3 → 7 записей. Ключевая находка —
  «ниже» достигается МЕНЬШИМ x, а не бо́льшим y при том же x: хребет —
  диагональ, у x=0 он уже на y=585 (небо там тянется почти до низа
  кадра), у x=250 — только до y=255. Взять тот же x, что у
  существующего облака, и просто увеличить y — риск оказаться НИЖЕ
  хребта (гора перекроет облако, так как оно не клипуется и рисуется
  ДО тела горы). Новые 4 позиции подобраны на малых/средних x (25-190)
  специально ради этого — проверены по клиренсу от хребта, везде
  >100px запаса. `cloudShape()`/`cloudsMarkup()`/анимация
  `cloud-drift` не менялись.
- **Тур убран целиком**: `cairnMarkup()`, вызов в
  `buildMountainSvg()`, `.mountain-cairn-stone` — всё удалено.
- **Туман вместо него**: `fogMarkup()`/`FOG_POSITIONS` — 4 широких
  плоских полупрозрачных «вздоха» (2 эллипсы на слой, шире и площе
  облака — туман стелется, а не пушится вверх). В отличие от тура (и
  водопада до него), **сознательно НЕ клипован** — рендерится СНАРУЖИ
  клипованной группы тела горы (как облака/птицы), а не внутри: резкий
  обрез по границе силуэта выглядел бы как геометрическая фигура, а не
  атмосферная дымка — у настоящего тумана нет чёткого края, лёгкий
  заход за хребет в небо — это нормально и естественно. Низкая
  непрозрачность (0.22, в тёмной теме ещё ниже — 0.12, тот же паттерн
  `@media`+`[data-theme]`, что уже даёт `.mountain-cloud`). Новый
  `@keyframes fog-drift` — медленнее и шире (40-58s на слой), чем
  `cloud-drift` (24-33s) — туман «тяжелее» и ленивее облака.
  `prefers-reduced-motion` отключает анимацию.
- Подтверждено скриншотами (светлая и тёмная тема): облака теперь
  видны по всей высоте неба, включая середину и низ, камней нигде нет,
  вместо них — мягкая дымка над каменной зоной, не перекрывающая
  подписи вех/тропу.
- `test-mountain-scenery.mjs`: облаков теперь 7 (было 3) + проверка на
  реальный разброс по y (не только count); весь раздел про тур заменён
  на раздел про туман (4 слоя, `fog-drift` анимация, низкая opacity,
  Y-диапазон, регрессия «НЕ внутри клипованной группы» — обратная
  проверке, что раньше требовалась для тура/водопада,
  `prefers-reduced-motion`); проверки «водопад удалён»/«тур удалён» (0
  узлов обоих классов); поправлена общая проверка границ viewBox и
  «не пересекается по классам» (`.mountain-cairn-stone` →
  `.mountain-fog`). Итого 89 проверок (было 83). Итого 528 (полный
  набор), все проходят. `sw.js`'s `CACHE_NAME` — v18 → v19.

Шестнадцатый заход (та же сессия) — «полностью убрать барашков с горы».
Прямая просьба убрать всех 5 барашков целиком: SVG, все 4 анимации
жизни (выпас, моргание, покачивание тела, подёргивание ушей),
тап-интерактивность (прыжок + сердечко). Остальной декор (деревья,
трава, тропа, птицы, облака, туман) не трогать.
- **`js/mountainSvg.js`**: удалён весь блок «Sheep» целиком —
  `SHEEP_POSITIONS`, `WOOL_LUMPS`, `SHEEP_LIFE_ANIM`,
  `sheepNoseMarkup()`, `sheepEarMarkup()`, `sheepShape()`,
  `sheepMarkup()`, экспорт `sheepHeartPopMarkup()`. Заодно удалена
  `pointBelowRidge()` — с прошлого захода, где деревья/трава перешли на
  `densityGrid()`, барашки были её единственным оставшимся вызывающим;
  после их удаления функция стала мёртвым кодом, убрана вместе со своим
  doc-комментарием (та же гигиена, что уже применялась в сессии к
  другим осиротевшим функциям типа `jitteredGreenGrid()`). Комментарий
  над `RIDGE_SAFETY_MARGIN` (ссылался на `pointBelowRidge()` по имени)
  переписан — константа по-прежнему нужна `densityGrid()`. В
  `buildMountainSvg()` убраны `${sheepMarkup()}` из тела SVG и
  `<radialGradient id="sheepWoolGradient">` из `<defs>`. Попутно
  зачищена одна осиротевшая перекрёстная ссылка на `sheepShape()`
  внутри doc-комментария `birdShape()` («та же логика, что уже
  применяет treeShape()/sheepShape()» → только `treeShape()`).
- **`css/mountain.css`**: удалён весь блок между `.mountain-grass` и
  `.mountain-trail` — все `.mountain-sheep*` правила, все 4
  `@keyframes sheep-graze/-blink/-body-sway/-ear-twitch` с их
  `--active`-модификаторами, `@keyframes sheep-hop`/`sheep-heart-pop` и
  оба их `prefers-reduced-motion`-блока. Отдельно удалены
  `.mountain-gradient-stop--wool-center`/`--wool-edge` (стопы цвета
  удалённого `sheepWoolGradient`, лежали в самом начале файла рядом с
  другими `--*-gradient-stop`-классами, вне только что удалённого
  диапазона).
- **`js/views/mountainView.js`**: убран `sheepHeartPopMarkup` из
  импорта `../mountainSvg.js`; удалены `wireMountainSheep()`,
  `triggerSheepTap()` целиком и вызов `wireMountainSheep(container)` в
  `renderFreshMountainView()`.
- Подтверждено скриншотами (светлая и тёмная тема): барашков нигде на
  горе нет, деревья/трава/тропа/птицы/облака/туман — на месте, визуально
  не изменились. Прямая DOM-проверка:
  `document.querySelectorAll(".mountain-sheep")` и любой
  `[class*="mountain-sheep"]` — 0 узлов; клик по месту, где раньше
  сидел барашек, не создаёт ни `.mountain-sheep-hop`, ни
  `.mountain-sheep-heart-pop`.
- `test-mountain-scenery.mjs`: удалён весь раздел «5: sheep» (структура
  барашка, привязка к теме, 5 разных анимаций жизни, порядок рендера
  после деревьев) и раздел «6: tap interaction» (тап → прыжок +
  сердечко, барашек не удаляется), убраны 4 проверки
  `prefers-reduced-motion` на `sheep-graze/-eyes/-body-sway/-ear`,
  `.mountain-sheep-wool` убран из общего селектора границ viewBox,
  упрощена проверка «fog/birds не делят классы» (пункт про
  `mountain-sheep` заменён на самопроверку `mountain-fog`, барашков
  больше нет как класса для сравнения), поправлена формулировка
  комментария у проверки числа деревьев («grass/sheep untouched» →
  «grass untouched»). Итого 59 проверок в файле (было 89 — ушёл весь
  блок про барашков и тап). Полный набор `test-*.mjs`: 498/498 (29
  файлов), все проходят. Бандл (`build-preview.mjs`) пересобран и
  проверен напрямую (`file://`) — 0 барашков, остальной декор на
  месте. `sw.js`'s `CACHE_NAME` — v19 → v20.

Семнадцатый заход (та же сессия) — «календарь должен открываться на
сегодняшнем дне, а не на первом числе». Уточнил у пользователя: в коде
два разных «календаря месяца» — вертикальный скроллящийся список
дней-пилюль на экране одной привычки (`js/views/calendarView.js`,
«Привычки» → тап по привычке) и компактная сетка `.calendar-grid` на
вкладке «Прогресс» (весь месяц виден сразу, сегодня просто подсвечен —
скроллить там нечего). Пользователь подтвердил: чинить только список у
привычки.
- **`js/views/calendarView.js`**: `renderCalendarView()` теперь
  различает «свежее открытие» экрана этой привычки (переход с другого
  экрана/другой привычки) и обычный ре-рендер уже открытого экрана —
  ре-рендер случается на каждое изменение состояния где угодно в
  приложении (`subscribe(render)` в `js/app.js`) и на клик
  `cal-prev`/`cal-next`. Различение — по DOM, ДО перезаписи
  `container.innerHTML`: `.day-pill-list` теперь несёт
  `data-habit-id="${habitId}"`, и «свежее открытие» — это когда такого
  узла с тем же `habitId` ещё нет (либо экран открыт первый раз, либо
  до этого был показан другой экран/другая привычка). Только в этом
  случае, после того как список отрисован, `.day-pill.is-today`
  (уже существовавший класс) ищется и подтягивается в видимую область
  через `scrollIntoView({ block: "center" })` — без `smooth`, экран
  должен открываться уже там, а не показывать анимированную прокрутку.
  Обычный ре-рендер (отметка дня где угодно в приложении, навигация по
  месяцам) скролл не трогает — иначе он выдёргивал бы пользователя из
  того места списка, где он реально листает.
- Плейсменты `viewMonthByHabit` (Map, помнит просматриваемый месяц на
  время жизни страницы) не менялись — если пользователь перелистнул на
  другой месяц и вернулся к той же привычке позже, «свежее открытие»
  снова истинно, но `.day-pill.is-today` в показанном (не текущем)
  месяце просто не найдётся — скролл корректно не сработает.
- Подтверждено Playwright: открытие привычки (сегодня — 26-е число)
  сразу показывает `.day-pill.is-today` в видимой области
  `#view-container` без ручного скролла; отметка дня выполненным в
  любом месте приложения (`setEntry()` напрямую, в обход клика — чтобы
  не путать с нативным поведением браузера «прокрутить фокуснутую
  кнопку в видимую область», которое сработало бы независимо от этой
  правки) не сбрасывает позицию скролла обратно к сегодня; клик
  `cal-prev`/`cal-next` тоже не дёргает скролл; уход с экрана и
  повторное открытие той же привычки заново скроллит к сегодня. Полный
  набор `test-*.mjs`: 498/498, без регрессий. Бандл (`build-preview.mjs`)
  пересобран и проверен напрямую (`file://`) — тот же эффект.

Восемнадцатый заход (та же сессия) — «добавить фото-баннер в раздел
«Достижения» + скруглить углы». Пользователь залил
`assets/habits/Achievements.png` (золотые кубок/щит/корона/монета/
звезда на зелёном градиенте, 1683×935) отдельным коммитом на GitHub.
Уточнил у пользователя: наверху экрана уже стоит маленькая
иконка-рамка с кубком (`.illustration-frame` + `trophyIllustration()`,
96×96) — подтвердил, заменить её новым баннером, а не показывать оба.
- **Оптимизация фото — не только косметика**: `build-preview.mjs`
  (сборщик Artifact-превью, в scratchpad) инлайнит вообще все файлы
  `assets/habits/*` как base64 без разбора, используются они или нет;
  бандл уже был ~15.45 МБ при потолке Artifact в 16 МБ. Добавить
  исходный PNG как есть (1.6 МБ → ~2.1 МБ в base64) почти наверняка
  переполнило бы лимит. Пересжал (Pillow): ресайз до 1440px по ширине
  (с запасом покрывает `max-width: 480px` контентной колонки на 3×
  DPR) + JPEG q85 → **~95 КБ** вместо 1.6 МБ (фото — гладкий градиент,
  идеальный случай для JPEG, альфа-канала не было). Итоговый файл —
  `assets/habits/achievements_banner.jpg`, исходный
  `Achievements.png` удалён (остаётся в истории git).
- **`js/illustrations.js`**: новая `achievementsBannerHtml()` — свой
  простой `.achievements-banner`-контейнер (не `.habit-hero-photo`,
  тот заточен под обрезанный full-bleed кадр 4:5 — здесь нужна вся
  картинка целиком, без обрезки, чтобы ни один из пяти значков не
  срезался по краю). `trophyIllustration()` осталась экспортирована и
  используется — не осиротела, а стала fallback-иллюстрацией:
  `wirePhotoFallback()` получил ветку `data-fallback-achievements` →
  `trophyIllustration()`, тот же принцип «всегда есть тематическая
  иллюстрация, фото или нарисованная», что и у остальных фото в файле.
- **`css/illustrations.css`**: `.achievements-banner` — `width:100%`,
  `border-radius: var(--radius-lg)` (тот же токен, что у
  `.media-card`/`.illustration-frame`), `overflow:hidden`,
  `box-shadow: var(--shadow-sm)`; `<img>` внутри — `width:100%;
  height:auto` (не `object-fit:cover` с фиксированным aspect-ratio,
  как у `.media-card` — здесь важно сохранить пропорции без обрезки).
- **`js/views/achievementsView.js`**: `<div class="illustration-frame">
  ${trophyIllustration()}</div>` заменён на `${achievementsBannerHtml()}`.
- Один баг по пути: первая версия `achievementsBannerHtml()` вписывала
  имя файла прямо в строку (`src="./assets/habits/achievements_banner.jpg"`),
  а не через переменную `filename` в шаблонной строке — из-за этого
  `build-preview.mjs`'s инлайнер (ищет ровно паттерн
  `src="./assets/habits/${filename}"`, как у `heroPhotoFrame()` и
  соседей) не находил и не патчил её, в Artifact-бандле картинка
  404-илась и откатывалась на fallback-SVG. Поймано на шаге проверки
  бандла, поправлено — `filename` вынесен в переменную, как у всех
  остальных фото-билдеров в файле.
- Заодно расширен сам `build-preview.mjs` (только scratchpad, не в
  репозитории): инлайнер фото раньше подхватывал только `*.png` и
  хардкодил `image/png` MIME — теперь берёт и `.jpg`/`.jpeg`, выбирая
  MIME по расширению.
- Подтверждено Playwright (светлая и тёмная тема, скриншоты): баннер на
  всю ширину контентной колонки, углы скруглены (`--radius-lg`),
  прежней маленькой иконки-кубка на этом экране больше нет; на
  «Настройках»/«Испытаниях» их собственные `.illustration-frame`
  не тронуты; картинка адаптивно тянется на 360/430/600px без
  искажения пропорций; подмена `src` на несуществующий файл корректно
  откатывается на `trophyIllustration()`. Полный набор `test-*.mjs`:
  498/498, без регрессий. Бандл пересобран и проверен напрямую
  (`file://`) — реальное фото показывается как inlined data URI (не
  fallback-иконка), итоговый размер 15.57 МБ (было 15.45 МБ, +~120 КБ)
  — в пределах лимита в 16 МБ. `sw.js`'s `CACHE_NAME` — v20 → v21.

Девятнадцатый заход (та же сессия) — «доработка раздела «Настройки»:
иконка заголовка, кастомный дропдаун, «О приложении»».
- **Иконка заголовка**: старый `gearIllustration()` — тонкий тёмный
  контур с мелкими прямоугольными зубцами + зелёный «листик» внутри —
  на маленьком размере читался неоднозначно (жалоба пользователя:
  «похоже на лампочку»). Переписан на один сплошной path (10 зубцов +
  `fill-rule="evenodd"` отверстие в центре — настоящая прозрачность,
  не закрашенный кружок, поэтому без шва просвечивает плашка под ним),
  проверен рендером до применения — однозначно читается как шестерёнка
  и в светлой, и в тёмной теме. Плашка `.illustration-frame` получила
  модификатор `.illustration-frame--accent` (только на «Настройках» —
  `.illustration-frame` общий с «Испытаниями», их не трогал) с тем же
  зелёным градиентом, что уже даёт `.settings-row-icon` и
  `.day-pill.is-today` (`linear-gradient(135deg, var(--color-accent),
  var(--color-accent-dark))`) — не новый цвет, переиспользование
  устоявшегося языка приложения. Заливка глифа —
  `var(--color-accent-contrast)` (тот же токен, что «текст на зелёной
  заливке» в `.today-check` — белый в светлой теме, тёмный в тёмной,
  контрастен в обеих).
- **Кастомный дропдаун**: проверил оба элемента, которые пользователь
  назвал «выпадающими списками». Тема (`#theme-picker`) — уже не
  нативный select, а полностью кастомный `.segmented` (зелёный акцент
  для активного пункта, синего нигде нет) — не тронут. Единица
  измерения по умолчанию (`#default-unit-select`) — настоящий нативный
  `<select>`; всплывающий список нативного select рисует браузер/ОС,
  приложение физически не может перекрасить его синее выделение через
  CSS — единственный способ дать «закруглённые углы, мягкие тени,
  зелёный акцент» — заменить на кастомный виджет. Новый
  `.settings-dropdown` (`js/views/settingsView.js`'s
  `wireUnitDropdown()`) — кнопка-триггер (текущее значение + эмодзи из
  уже существующего `UNIT_ICONS`, `js/logic/units.js`, тот же
  справочник, что и у пикера единиц в форме привычки) + абсолютно
  спозиционированное меню (`border-radius: var(--radius-md)`,
  `box-shadow: var(--shadow-md)`, скролл при переполнении — 10
  пунктов). Выбранный пункт — `rgba(var(--color-accent-rgb), .14)` фон
  + `var(--color-accent-dark)` текст вместо синего. Открытие/закрытие:
  клик по триггеру, клик вне меню, Escape, выбор пункта. Слушатель
  клика вне меню на `document` вешается **только пока меню открыто** и
  снимается при любом закрытии — экран целиком перестраивается на
  каждое изменение состояния (`subscribe(render)`), задвоенный
  слушатель на `document` иначе копился бы на каждый выбор единицы.
- Один баг по пути, пойманный на скриншоте: `<button>` без явного
  `border`/`background` в CSS сохраняет нативную рамку браузера —
  `.settings-dropdown-option` изначально не переопределял ни то, ни
  другое (в отличие от `.segmented button`, который явно это делает),
  из-за чего пункты меню показывали уродливую системную рамку кнопки
  поверх зелёной подсветки. Поправлено — `border: none; background:
  none;` явно добавлены.
- Второй баг, пойманный тестами: `.settings-dropdown-option:hover`
  был добавлен вне `@media (hover: hover) and (pointer: fine)` —
  нарушал устоявшееся во всём приложении правило (все `:hover` вешают
  только на устройства с реальным hover, чтобы тач-тап не оставлял
  «залипший» hover-стиль). Перенесено в существующий hover-блок
  `components.css`.
- **«О приложении»**: блок уже существовал — добавлена вторая строка
  внутри той же карточки, `.settings-about-text` (обёртка, несёт
  `flex:1` вместо прежнего прямого правила `.settings-row >
  .modal-message` — текст описания больше не прямой потомок
  `.settings-row`) → `<p class="modal-message">` (исходное описание) +
  `<p class="settings-about-credit">Приложение разработано Капустиным
  Николаем</p>` (типографика как у `.settings-row-desc`).
- Подтверждено Playwright (светлая и тёмная тема, скриншоты): иконка
  читается как шестерёнка на зелёном градиенте в обеих темах; тема-
  пикер работает как раньше (регрессия); ни одного нативного `<select>`
  не осталось на странице; клик по триггеру открывает меню (10
  пунктов, скруглённые углы, тень); выбор пункта обновляет
  `localStorage` и подпись триггера, закрывает меню; клик вне меню
  закрывает; 5 циклов открыть/выбрать подряд не оставляют побочных
  эффектов (проверка на отсутствие утечки слушателей); блок «О
  приложении» показывает обе строки. `test-settings.mjs`: строка с
  `page.selectOption("#default-unit-select", ...)` заменена на клик по
  триггеру + клик по пункту меню — остальной тест (предзаполнение формы
  новой привычки выбранной единицей, экспорт, сброс аккаунта) не
  менялся. Полный набор `test-*.mjs`: 498/498 (после исправления
  hover-регрессии, пойманной этим же прогоном). Бандл
  (`build-preview.mjs`) пересобран и проверен напрямую (`file://`) —
  тот же результат. `sw.js`'s `CACHE_NAME` — v21 → v22.

Двадцатый заход (та же сессия) — «добавить интерактивное обучение
(онбординг)». Самая крупная фича этой сессии: пошаговый тур, каждый шаг
подсвечивает один реальный элемент интерфейса (остальное затемнено),
шаг завершается реальным действием пользователя на этом элементе (не
кнопкой «далее»), автозапуск при первом визите, кнопка «Пропустить» в
углу, финальный экран, пункт «Показать обучение заново» в Настройках.
Пользователь предложил 6 шагов и явно разрешил скорректировать их «по
факту устройства UI».

- **Расхождение с реальным UI, решено самостоятельно**: пользователь
  описал шаг 3 как «галочка на карточке привычки», но на экране
  «Привычки» карточка несёт только декоративное кольцо прогресса
  (`statusRing()`), не интерактивную кнопку. Настоящая «отметка
  выполнения» живёт в блоке «Сегодня» на экране «Прогресс»
  (`mountainView.js`'s `todayListHtml()`) — кнопка `.today-check` для
  простых привычек, поле ввода числа для числовых (у числовых сама
  кнопка-галочка там `aria-hidden`, без обработчика). Решение: после
  того как шаг 2 («+») реально создаёт привычку, онбординг сам
  переключает `location.hash` на `#/progress` и подсвечивает РЕАЛЬНЫЙ
  контрол именно этой новой привычки — порядок и число шагов (6) не
  изменились, просто шаг 3 физически происходит на «Прогрессе», а не
  на «Привычках»; шаг 4 (гора) естественно продолжается там же.
- **Архитектура валидирована отдельным Plan-агентом перед реализацией**
  (эта фича — самая крупная в сессии, сначала собрал полный контекст
  чтением кода, затем отдельный агент проверил/уточнил план перед
  началом кода). Ключевая находка: `app.js`'s `render()` полностью
  перестраивает и таб-бар, и текущий экран на КАЖДОЕ изменение
  состояния и на каждый `hashchange` — значит, оверлей обучения не
  может держать DOM-ссылки между шагами (всегда ищет цель заново) и не
  должен вешать собственные независимые `subscribe()`/`hashchange`
  слушатели (риск гонки очерёдности) — вместо этого один терминальный
  хук `onOnboardingTick()`, вызываемый в самом конце `render()`, после
  того как DOM уже точно перестроен для этого прохода.
- **`js/components/onboarding.js`** (новый файл, основная логика):
  `startOnboarding({force})` (не автозапускается повторно, если флаг
  уже `true`, если явно не `force`), `onOnboardingTick()` (экспорт для
  `app.js`), `skipOnboarding()`. Список из 6 шагов (`buildSteps()`) —
  таб-бар целиком → `#add-habit-fab` → контрол новой привычки в
  «Сегодня» (тип зависит от `habit.type`) → `.mountain-wrap` →
  вкладка «Испытания» → вкладка «Достижения». У каждого шага —
  `findTarget()` (селектор/функция, `null` = «подождать» — до 15
  повторов по 100мс, затем шаг пропускается, а не подвешивает тур
  навсегда), `isComplete()` (условие завершения — переход
  false→true, не диф против собственного мутируемого состояния,
  та же защита от «больше одного `subscribe(render)` прохода за тик»,
  что уже потребовалась однажды в `mountainView.js`'s bounce-анимации).
  Условия завершения — по типу: реальная навигация
  (`location.hash === "..."` для таб-бара/вкладок Испытания/
  Достижения), реальное состояние (`getHabits().length` выросло для
  «+»; `isDayComplete()` для отметки — общее для галочки и числового
  поля, не завязано на конкретный клик), и один особый случай — гора:
  единственный шаг без реального действия (в отличие от шагов 2/3,
  где пользователь явно просил «дождаться нажатия/отметки» — для этого
  шага в просьбе такой формулировки нет, только «объяснить»), поэтому
  авто-переход через ~2.5с чтения, тап по подсветке — досрочно.
  **Важный нюанс, пойманный при первом прогоне**: тап по горе/срабатывание
  таймера сами по себе не запускают `render()` (не мутируют состояние,
  не меняют `hash`) — значит, ничего не перепроверило бы условие шага,
  тур завис бы на месте (и подтверждено тестом — следующий клик по
  реальной вкладке блокировался НЕПРАВИЛЬНО ОСТАВШИМСЯ скримом горы).
  Исправлено — оба места (клик и таймер) сами вызывают внутренний
  `runTick()` сразу после выставления флага, вместо того чтобы ждать
  чужого рендера.
- **Оверлей — 4 полосы-скрима**, не `clip-path`: независимые
  `position: fixed` прямоугольники (top/bottom/left/right) вокруг
  `getBoundingClientRect()` цели — вырез это просто отсутствие
  элемента там, реальный клик по подсвеченной кнопке проходит нативно,
  без `pointer-events`-трюков. Пересчёт: на активации шага, на каждый
  `onOnboardingTick()`, на `resize`; при «цель не найдена» —
  геометрия просто не трогается (замирает на прошлой), это же
  естественно решает опасение «мелькнёт неверный вырез, пока открыта
  модалка» без отдельной защиты специально под модалку — сама
  модалка просто перекрывает всё оверлеем поверх (z-index).
- **z-index**: новая `--z-onboarding: 90` в `variables.css` — между
  `--z-tabbar: 50` и `--z-modal-backdrop: 100`. Настоящая модалка
  добавления привычки (шаг 2) естественно перекрывает оверлей обучения
  собственным backdrop — подтверждено тестом на сравнение вычисленных
  z-index.
- **`css/onboarding.css`** (новый файл): `.onboarding-scrim-band` ×4,
  `.onboarding-spotlight-ring` (зелёная обводка, `pointer-events:none`),
  `.onboarding-tooltip` (карточка в стиле `.card`, позиционируется
  над/под целью по доступному месту, счётчик «N из 6»),
  `.onboarding-skip-btn` (полупрозрачная пилюля в правом верхнем углу,
  с учётом `env(safe-area-inset-top)`, тот же паттерн, что у
  `.fab`/`.habit-hero-back` для широких viewport —
  `right: calc(... + max(0px, (100% - 480px) / 2))`),
  `.onboarding-completion` (отдельный полноэкранный финальный экран).
- **`js/state/storage.js`**: новое поле `meta.onboardingCompleted`.
  Важная асимметрия дефолтов (не typeof-проверка, как у остальных
  полей): в `emptyState()` (истинно первый визит) — `false`; в
  `migrate()` (любое существующее сохранение, включая старое, где
  этого поля ещё не было) — `raw.meta.onboardingCompleted !== false`
  (т.е. дефолт `true` для всего, кроме явного сохранённого `false`) —
  иначе каждый существующий пользователь увидел бы тур неожиданно
  после апдейта. Единственный случай, где старое сохранение легитимно
  несёт `false` — новый пользователь перезагрузил страницу до
  завершения тура, для него флаг должен остаться `false`.
- **`js/app.js`**: `startOnboarding()` вызывается один раз при загрузке
  (та же позиция, что и `maybeShowMonthRecap()` — после первого
  `render()`); `onOnboardingTick()` — безусловно в самом конце
  `render()`.
- **`js/views/settingsView.js`**: новый раздел «Обучение» с кнопкой
  «Показать обучение заново» (`startOnboarding({force:true})`) —
  между «Данные» и «О приложении». Новая `iconHelp()` в
  `illustrations.js` (иконка «повтор» — круговая стрелка, проверена
  рендером до вшивания в код, как и шестерёнка в прошлый заход).
- **Повтор при уже существующих привычках**: если на старте тура
  `getHabits().length > 0`, шаг «+» пропускается целиком (не просто
  «уже выполнен» — вообще убран из списка, итого 5 шагов, не 6, это
  осознанно), шаг отметки сразу целится в первую существующую
  привычку; если она уже отмечена сегодня — короткая пауза (~900мс,
  тот же `advanceDelayMs`, что и для «уже было true» на любом другом
  прогоне) вместо требования повторного тапа.
- **Регрессия на существующем наборе тестов, пойманная и исправленная**:
  42 файла в scratchpad-наборе начинаются с `localStorage.clear()` —
  ни один не был написан с учётом онбординга, поэтому первый полный
  прогон почти полностью завис (тур блокирует клики по нужным тестам
  элементам своим скримом). Исправлено единой механической заменой во
  всех файлах: `localStorage.clear()` → `(localStorage.clear(),
  localStorage.setItem("habit-mountain:v1", JSON.stringify({ meta: {
  onboardingCompleted: true } })))` (безопасно проходит через
  `migrate()` — минимальный объект `{meta:{onboardingCompleted:true}}`
  корректно достраивается до полного пустого состояния с нужным
  флагом). Ожидаемая, а не случайная цена фичи — как и для любого
  реального продукта с первым визитом, требующим такой правки
  существующего набора тестов.
- Новый постоянный `test-onboarding.mjs` (31 проверка): полный
  прогон с нуля (создание привычки, отметка галочкой — отдельно для
  простой и числовой привычки, тап по горе, переходы по вкладкам,
  финальный экран, флаг в `localStorage`, отсутствие повторного
  автозапуска), skip-кнопка, повтор через Настройки с уже
  существующей привычкой (5 шагов), регрессия миграции (старое
  сохранение без поля `onboardingCompleted` не запускает тур).
  Подтверждено Playwright визуально (скриншоты, светлая/тёмная тема,
  360px/430px viewport — подсказка не вылезает за края экрана).
  Итого полный набор `test-*.mjs`: 529/529 (498 существующих + 31
  новых), без регрессий. Бандл (`build-preview.mjs`) пересобран
  (добавлен `onboarding.css` в `cssOrder`, `#onboarding-root` в
  захардкоженную HTML-заготовку бандлера) и проверен напрямую
  (`file://`) — полный прогон тура работает, 15.6 МБ, в пределах
  лимита. `sw.js`'s `CACHE_NAME` — v22 → v23.

Двадцать первый заход (та же сессия) — «сделать баннер «Achievements»
кликабельным». Баннер (`achievements-banner`, фото наград) на экране
«Достижения» был чисто декоративным — теперь по клику открывает
модалку со сводкой: сколько наград получено из скольки всего, прогресс
до ближайшей, список полученных с датами. Без мотивационных фраз —
только факты, как и просил пользователь.

- **Находка при чтении кода**: в приложении уже была полноценная
  система из 7 фиксированных «достижений»
  (`js/logic/achievements.js`'s `ACHIEVEMENTS`/`evaluateAndUnlock()`/
  `getUnlockedMap()`) — разблокируются как one-way ratchet с
  `unlockedAt`, персистятся в `state.meta.unlockedAchievements` и уже
  пересчитываются на каждый рендер `achievementsView.js`
  (`getAppStats()` уже вызывает `evaluateAndUnlock()` внутри себя) —
  но нигде в UI не отображались (экран «Достижения» показывал только
  пройденные ИСПЫТАНИЯ, `CHALLENGE_POOL`, отдельная от достижений
  система). Не пришлось изобретать новую модель данных — только
  показать уже существующую.
- **`js/logic/achievements.js`**: у каждого из 7 достижений уже был
  булев `check()` — добавлен параллельный `progress(s)` (0..1), из тех
  же полей `stats`. Новая `getNextAchievementProgress(stats)` — среди
  ещё не разблокированных берёт достижение с **наибольшим** текущим
  прогрессом (не первое по порядку объявления в массиве) — так
  пользователь видит, к чему он реально ближе всего. Возвращает `null`,
  если разблокированы все 7.
- **`js/views/achievementsView.js`**: `getAppStats()` (раньше
  результат отбрасывался) → используется для прогресса. Новая
  `openAchievementsSummary(stats)` строит модалку через уже
  существующий `openModal()` (`components/modal.js`, тот же компонент,
  что и везде в приложении) — максимальное переиспользование готовых
  стилей, ни одного нового модального компонента: счётчик — уже
  существующий `.stat-pill`/`.stat-value`/`.stat-label` (тот же
  большой градиентный номер, что на «Прогрессе»); прогресс до
  ближайшей — уже существующий `challengeProgressBadge()` (кольцо с
  процентом, обычно на карточках испытаний, тут крупнее) внутри
  `.card` + уже существующие `.settings-row-title`/
  `.settings-row-desc` для названия/описания; список полученных — уже
  существующий `<ul class="today-list">` (тот же, что и в
  `daySummaryView.js`) из `<li class="today-item">`, отсортированных
  по `unlockedAt` убыванием, дата — уже существующая
  `formatShortDate()` (та же функция, что уже форматирует даты у
  карточек пройденных испытаний в этом файле); пусто — уже
  существующий `.empty-state`-паттерн («Пока нет полученных наград.» —
  факт, не подбадривание). Из нового — только маленький круглый бейдж
  с эмодзи достижения на зелёном градиенте (`.achievements-summary-icon`,
  тот же приём, что `.settings-row-icon`, просто под эмодзи вместо
  инлайн-SVG) и три класса компоновки
  (`.achievements-summary-stat/-next/-next--complete`,
  `css/components.css`).
- **`js/illustrations.js`**'s `achievementsBannerHtml()`: обёртка
  `<div class="achievements-banner">` → `<button type="button">`
  (тот же приём, что уже даёт `.popular-habit-card` в
  `habitsView.js` — реальная `<button>`, оборачивающая
  фото-иллюстрацию, не `role="button"`-div с ручным keydown, как у
  `.habit-card` — клавиатурная активация Enter/Space работает
  бесплатно). `alt` у фото убран в пустую строку (фото декоративно
  внутри уже подписанной `aria-label`'ом кнопки — не дублировать для
  скринридера).
- **Заранее заложенный сброс кнопки** (не постфактум, как в прошлый
  раз): `.achievements-banner` в `css/illustrations.css` сразу получил
  явный `border: none; background: none; padding: 0; text-align: left;
  cursor: pointer;` — ровно та защита, которую в прошлый заход
  пришлось добавлять постфактум на `.settings-dropdown-option` после
  того, как скриншот поймал нативную браузерную рамку кнопки поверх
  дизайна. `:active`/`:hover` (последний — внутри `@media (hover:
  hover) and (pointer: fine)`, обязательное правило проекта) добавлены
  сразу же, в стиле уже существующего `.popular-habit-card:hover`.
- Подтверждено Playwright (светлая/тёмная тема, скриншоты): клик по
  баннеру открывает модалку; пустое состояние (0 достижений) — счётчик
  «0 из 7», кольцо прогресса, нейтральный пустой список; одно
  разблокированное — счётчик «1», строка с датой в списке; сценарий с
  привычкой на стрике 5 дней — подтверждено, что «ближайшее» — именно
  `streak_7` («Неделя силы», 71%), а не первое по порядку объявления в
  массиве (`streak_30` был бы первым по порядку, но дальше по
  прогрессу); все 7 разблокированы — факт «Все награды получены» без
  кольца на несуществующую цель, список отсортирован новыми сверху;
  клавиатурная активация (`Tab` + `Enter`) открывает модалку бесплатно
  от реальной `<button>`; нативная рамка кнопки не показывается
  (регрессия на уже пойманный баг). Новый постоянный
  `test-achievements-summary.mjs` (19 проверок) — все сценарии выше.
  Итого полный набор `test-*.mjs`: 548/548 (529 существующих + 19
  новых), без регрессий. Бандл (`build-preview.mjs`) пересобран и
  проверен напрямую (`file://`) — тот же результат, новых файлов не
  добавилось (все тронутые файлы уже были в `cssOrder`/сборке).
  `sw.js`'s `CACHE_NAME` — v23 → v24.

Двадцать второй заход (та же сессия) — «заменить текст в попапе
баннера «Достижения»». Точечная правка: заголовок попапа
(`openAchievementsSummary()`, `js/views/achievementsView.js`) —
`"Сводка достижений"` → `"Копилка твоих достижений"`, передаёт смысл
«здесь хранятся все твои достижения» (адаптировано под неформальное
«ты», как и весь остальной текст в приложении). Остальная структура
попапа (счётчик, кольцо прогресса до ближайшей награды, список
полученных с датами, пустое состояние) не тронута — как и просили.
Обновлена одна строка в `test-achievements-summary.mjs` (ожидаемый
заголовок), остальные 18 проверок в файле не менялись. Подтверждено
Playwright (светлая/тёмная тема, скриншоты) — заголовок читается,
разметка не съехала. Полный набор `test-*.mjs`: 548/548, без
регрессий. Бандл пересобран и проверен напрямую (`file://`).

Двадцать третий заход (та же сессия) — жалоба «текст в попапе
«Достижения» так и не изменился», диагностика + точечное исправление.
Полная read-only проверка (план-режим) показала: сам заголовок модалки
(`openAchievementsSummary()` → `title: "Копилка твоих достижений"`) в
коде, в git (закоммичен и запушен, `43d8e1a`) и в уже опубликованном
Artifact — везде корректен, второго похожего компонента/попапа в
приложении нет. Единственный реальный недосмотр прошлого захода:
`achievementsBannerHtml()` (`js/illustrations.js`) — сама
кнопка-баннер несла старый `aria-label="Сводка достижений"`, не
обновлённый вместе с заголовком модалки (расхождение не видно
визуально, но это единственное оставшееся вхождение старого текста в
кодовой базе) — исправлено на `aria-label="Копилка твоих
достижений"`. Вероятная причина того, что видел пользователь: PWA
кэширует приложение через `sw.js` (`fetch`-обработчик cache-first, сеть
не проверяется при попадании в кэш) — если пользователь смотрел на
установленное/ранее открытое приложение, а не на свежую вкладку с
Artifact-ссылкой, старая версия могла продолжать отвечать из кэша до
следующего обновления сервис-воркера. `CACHE_NAME` в `sw.js` бампнут
ещё раз (v25 → v26) как явный триггер обновления кэша у реальных
инсталляций. В `test-achievements-summary.mjs` добавлены 2 новые
проверки: `aria-label` баннера совпадает с заголовком модалки, и
строка «Сводка достижений» не встречается нигде в DOM после клика —
итого 21/21 в файле. Полный набор `test-*.mjs`: без регрессий (все
файлы прошли). Бандл пересобран; напрямую в собранном HTML
подтверждено grep'ом 0 вхождений старого текста и 2 вхождения нового
(заголовок + aria-label); Playwright-проверка того же бандла (`file://`)
подтвердила оба значения визуально/через DOM.

Двадцать четвёртый заход (та же сессия) — «убрать кликабельность
баннера «Achievements»» — полный откат фичи из 22-23 заходов. Баннер
снова стал простым статичным `<div>` без `aria-label` и интерактивных
состояний (было — `<button>`). Удалено целиком: обработчик клика и
функция `openAchievementsSummary()` (`js/views/achievementsView.js`,
вместе с ставшими ненужными импортами `getAppStats`, `openModal`,
`ACHIEVEMENTS`, `getUnlockedMap`, `getNextAchievementProgress`);
экспорт `getNextAchievementProgress()` и поле `progress` у всех 7
объектов в `ACHIEVEMENTS` (`js/logic/achievements.js` — `check` и
`evaluateAndUnlock()` не тронуты, разблокировка достижений в фоне
продолжает работать как и раньше через `mountainView.js`'s
`getAppStats()`, независимо от этого экрана); классы
`.achievements-summary-stat/-next/-next--complete/-icon`
(`css/components.css`); button-хром и `:hover`/`:active` состояния
`.achievements-banner` (`css/illustrations.css` — оставлены только
`display/width/margin/border-radius/overflow/box-shadow`, как у
баннера до кликабельности). Постоянный `test-achievements-summary.mjs`
удалён (проверял полностью убранную фичу), вместо него добавлен
`test-achievements-banner-static.mjs` (7 проверок): баннер — `<div>`,
не `<button>`; без `aria-label`; `cursor` не `pointer`; клик не
открывает модалку; элемент не фокусируется с клавиатуры; в DOM нет
классов `achievements-summary-*`; `data-fallback-achievements` на фото
по-прежнему работает. Полный набор `test-*.mjs` (31 файл, без
`test-achievements-summary.mjs`) — без регрессий. Бандл пересобран;
grep по собранному HTML — 0 вхождений `achievements-summary`,
`openAchievementsSummary`, `getNextAchievementProgress` и обеих
формулировок заголовка модалки; Playwright-проверка того же бандла
(`file://`) — баннер `<div>`, клик ничего не открывает, скриншот
подтверждает разметку без визуальных регрессий. `sw.js`'s
`CACHE_NAME` — v26 → v27.

Двадцать пятый заход (та же сессия) — «доработка карточек достижений в
разделе «Достижения»» — только секция «Пройденные испытания»
(«Незавершённые попытки» не трогалась). Причина жалобы «карточка
растянута почти на весь экран, медаль слишком крупная»:
`achievementCardHtml()` рендерила пройденные испытания тем же
full-bleed `.media-card` шаблоном, что и активные карточки испытаний —
фото-медаль растягивалась на всю ширину экрана как фон
(`aspect-ratio: 2.2/1`, `object-fit: cover`). Заменено на компактную
кликабельную строку: `achievementTileHtml()` — маленькая медаль
(56×56, тот же приём с `challengeHeroForId()`'s `position:absolute;
inset:0`, просто в маленькой рамке `.achievement-tile-medal`) + заголовок/
дата в `<button class="achievement-tile">` (реальная кнопка, не
`role="button"` div — клавиатурная активация бесплатно, по образцу уже
принятому в проекте для `.popular-habit-card`/бывшего
`.achievements-banner`). Несколько карточек теперь стекаются в
компактный список `.achievements-list` (`display:flex;
flex-direction:column`) вместо старого `.achievements-grid`, который
остался только за «Незавершёнными попытками». Клик по карточке
открывает попап `openAchievementDetail(challenge, info)` — зеркало уже
существующего `challengesView.js`'s `openChallengeDetail()`: заголовок
= название испытания, `.modal-message` с описанием, бейдж сложности
(`.challenge-difficulty-badge` + новый модификатор
`--standalone` — оригинальный класс рассчитан на полупрозрачную белую
плашку поверх фото, что на плоской поверхности модалки в тёмной теме
читалось бы плохо из-за завязанного на тему `--color-ink`; модификатор
меняет фон на `var(--color-surface-alt)` — цвет точки сложности не
трогается, он не завязан на фон плашки), и «Получено {дата}». Новые
CSS-классы — `css/components.css`: `.achievements-list`,
`.achievement-tile(-medal/-body)`, `.achievement-detail-medal/-meta`,
`.challenge-difficulty-badge--standalone`. Обновлены 2 существующих
теста, у которых сломались селекторы на переименованный класс
(`test-challenges.mjs`, `test-redesign-v5.mjs` — искали
`.achievement-card`/`.achievements-grid` там, где теперь
`.achievement-tile`/`.achievements-list`); добавлен новый постоянный
`test-achievement-card-detail.mjs` (17 проверок): карточка — компактная
кнопка (высота ≤100px, медаль ≤64px), клик открывает попап с верными
заголовком/описанием/сложностью/датой, второй тайл в списке открывает
свой собственный попап (не всегда первый), «Незавершённые попытки»
остались прежнего размера (регрессия). Полный набор `test-*.mjs`
(32 файла) — без регрессий. Бандл пересобран и проверен Playwright
(`file://`) — тот же сценарий, компактная карточка + корректный попап.
`sw.js`'s `CACHE_NAME` — v27 → v28.

Двадцать шестой заход (та же сессия) — **⚠️ ВРЕМЕННО** снято
ограничение «редактировать можно только последние 3 дня» (сегодня,
вчера, позавчера) — по просьбе, чтобы вручную наполнить тестовые
данные за любые прошлые/будущие даты. Ограничение реализовано в двух
местах, оба отключены точечными комментариями (сама логика окна не
удалена — только обход):
1. `js/logic/dateUtils.js`'s `isDateEditable(dateKey)` — фактический
   шлюз записи (единственный вызывающий —
   `js/state/entries.js`'s `setEntry()`). Тело временно заменено на
   `return true;`, оригинальные 2 строки оставлены закомментированными
   прямо под ней.
2. `js/views/calendarView.js`'s `dayPillHtml()` — UI-блокировка:
   `const locked = cell.isFuture || cell.isTooOld;` временно заменена
   на `const locked = false;` (оригинал закомментирован рядом) — это
   разом снимает `disabled`, класс `.is-locked` и «locked»-состояние
   кольца с любого дня, независимо от даты.

**Как вернуть ограничение обратно**: в обоих файлах раскомментировать
оригинальную строку(-и) и удалить временную `return true;`/
`const locked = false;` — помечено явным комментарием
«ВРЕМЕННО ОТКЛЮЧЕНО» на месте.

Подтверждено Playwright: клик по дню 10-дневной давности и по дню
через 2 дня в будущем — оба не `disabled`, без `.is-locked`, открывают
`openDayDetail()`, запись реально сохраняется в `localStorage`.
Полный набор `test-*.mjs`: **3 файла ожидаемо красные, пока флаг
снят** — все три по одной и той же причине (в текущем месяце больше
нет ни одного «залоченного» дня, чтобы её проверить), это не
регрессия:
- `test-editable-window.mjs` — прицельно тестирует само ограничение
  (что кнопки вне окна `disabled`, что `setEntry()` отказывает).
- `test-daypill-status-bug.mjs` — 3 из 18 проверок сверяли визуал
  именно `.is-locked` дня.
- `test-hover.mjs` — виснет на `page.locator(".day-pill.is-locked")`
  (элемента больше нет в DOM, раз залоченных дней не осталось).

Остальные 29 файлов — без изменений, зелёные. Тесты **не
переписывались** намеренно — они и есть проверка того, что
ограничение при откате снова заработает; править их сейчас значило бы
потерять эту подстраховку. Artifact-бандл **не пересобирался и не
публиковался** — эта правка предназначена для локального/деплойного
инстанса ради наполнения тестовых данных, а не для постоянного
демо-URL. Закоммичено и запушено как есть.

Двадцать седьмой заход (та же сессия) — «переделать обучение
(онбординг) — вступительный экран + свободный выбор вкладок». Старый
тур (`js/components/onboarding.js`) шёл жёсткой последовательностью
шагов (подсветка таббара → «добавь привычку» → «отметь её» → гора →
вкладка «Испытания» → вкладка «Достижения»), завязанной на то, есть ли
у пользователя привычки. Полностью переписан на новую машину состояний
(`active.phase`):
1. **`"welcome"`** — новый полноэкранный `mountWelcome()`: зелёный
   градиент (`.btn-primary`/`.fab`'s `linear-gradient(135deg,
   var(--color-accent), var(--color-accent-dark))`), 🏔️, заголовок
   «Готов покорить вершину?», подзаголовок, кнопка «Начать восхождение
   🚀» (сплошная белая с тёмно-зелёным текстом — не `.btn-primary`, тот
   зелёный градиент слился бы с таким же зелёным фоном) и ссылка
   «Пропустить обучение».
2. **`"spotlight"`** — переиспользован существующий bands/ring/tooltip-
   механизм (`mountRoot()` → `mountSpotlight()`), нацеленный на
   `.tabbar-inner`, текст «Нажми на любую вкладку внизу — необязательно
   по порядку, выбирай сам». Вместо старого пошагового `isComplete` —
   прямой клик-листенер на каждый `.tab-item`: любой клик сразу
   переводит в `"roam"` и засчитывает эту вкладку. Прямой листенер, а
   не диффинг `location.hash`, специально — клик по уже активной
   вкладке (тур мог стартовать прямо на «Прогресс») не порождает
   `hashchange` и был бы пропущен диффингом.
3. **`"roam"`** — свободная навигация, `#onboarding-root` пустой. В
   `onOnboardingTick()`: если текущая вкладка ещё не в `visitedTabs` —
   открыть её попап (`openModal()`, тот же `.modal-actions` с
   «Понятно»/«Пропустить обучение», что уже использует `openConfirm()`
   в `modal.js`) и записать как посещённую. Текст под каждую вкладку —
   новый `TAB_INTROS` (2-3 предложения по сути раздела: Испытания —
   как получить медаль; Привычки — «+» и отметка; Прогресс — гора и
   календарь; Достижения — где хранятся награды; Настройки — что можно
   настроить). Посещение всех 5 → после закрытия попапа последней —
   сразу `showCompletion()`.
4. **`"completion"`** — переиспользован `.onboarding-completion` почти
   как есть, текст под новую рамку («Теперь ты знаешь, где что искать
   — все разделы открыты»), подсказка про Настройки → «Показать
   обучение заново» осталась.

Убрана вся зависимость от `getHabits`/`getEntry`/`isDayComplete`/
`todayKey` — тур больше не завязан на состояние привычек пользователя.
`js/app.js` (контракт `startOnboarding()`/`onOnboardingTick()`) и
`js/views/settingsView.js`'s кнопка replay (`force: true`) не
трогались — тот же API. `css/onboarding.css`: добавлены
`.onboarding-welcome*` (welcome-экран), убран мёртвый
`.onboarding-tooltip-step` (счётчик «N из M» — больше не нужен, теперь
это не пронумерованная последовательность). Старый
`test-onboarding.mjs` (тестировал убранную пошаговую логику) заменён
на новый (32 проверки): welcome первым, клик по НЕ первой по списку
вкладке («Настройки») доказывает отсутствие навязанного порядка,
повторный заход на посещённую вкладку не открывает попап повторно,
все 5 вкладок → автозавершение, skip с любой из трёх фаз
(welcome/spotlight/попап вкладки) сразу помечает
`onboardingCompleted`, replay через Settings с уже существующими
привычками показывает welcome заново, миграция сохранений без поля
`onboardingCompleted` не трогалась (не в этом файле). Полный набор
`test-*.mjs`: без новых регрессий — 3 файла
(`test-editable-window.mjs`, `test-daypill-status-bug.mjs`,
`test-hover.mjs`) по-прежнему красные по не связанной с этим заходом
причине (временно снятое окно редактирования, см. предыдущий заход).
Бандл пересобран и проверен Playwright (`file://`) — тот же сценарий.
`sw.js`'s `CACHE_NAME` — v28 → v29.

## Файловая карта (кратко)

```
index.html, manifest.json, sw.js
css/variables.css      — палитра, спейсинг, тема
css/base.css           — сброс, layout, анимация перехода между экранами
css/components.css     — кнопки, карточки, таббар, hover, испытания, настройки
css/mountain.css       — гора (геометрия — в JS, здесь только стили/цвета)
css/calendar.css       — календарные сетки
css/illustrations.css  — цвета для js/illustrations.js
js/app.js              — роутинг, boot, тема
js/illustrations.js    — hand-drawn inline SVG
js/mountainSvg.js       — геометрия горы/тропы/вех
js/state/               — storage (схема+миграция), habits, entries, derive
js/logic/                — dateUtils, completion, streaks, points, progress,
                            achievements, challenges, units, theme
js/views/                — mountainView, habitsView, calendarView,
                            dayDetailView, daySummaryView, achievementsView,
                            challengesView, settingsView
js/components/           — tabBar, modal, toast, monthSummary, monthRecap
```
